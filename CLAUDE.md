# Lanyard Framework

## Project Intent
Framework for quickly building classroom games where:
- Teacher projects a "host screen" to the class
- Students interact via their devices (Chromebooks — phones are banned in most schools; design for Chromebooks/tablets)
- AI facilitates/processes collective input
- Games are text-based for simplicity (plus a stroke-based drawing input — the one deliberate exception)

## Target Users
- Novice coder (me) creating games via "vibe coding" with Claude
- Eventually: other teachers using the framework

## Architecture Principles
- Separation of Concerns: engine, views, AI are independent modules
- State Machine Pattern: every game is phases + transitions
- Events, Not Direct Calls: modules communicate via event bus
- AI Gets Summaries: collect data in scripts, send summary to AI (Script -> LLM Handoff)
- **Recipe layer:** teachers pick a recipe + fill 2-5 params; the compiler emits a phase graph (see `docs/RECIPE-LAYER.md`). Recipes are JSON, not code.
- **Custom creation is bricks-only:** recipe match or storyboard → `compileStoryboard`. The legacy whole-config generator is gone; if an activity can't be assembled from validated bricks, it can't be made.

## Code Principles
- Don't swallow errors — surface them clearly
- No hardcoded values — use config
- Each file does one thing
- Write tests before implementation (TDD)
- Keep it simple — vanilla JS, no frameworks, no build steps

## Key Technical Decisions
- Node.js (v24, ES modules) + Express 5.x (stricter path matching than Express 4)
- Socket.io for real-time communication
- Vanilla HTML/CSS/JS for screens (no React); editor code uses `var` — match that style
- Vitest for testing
- Claude API for AI (Haiku 4.5 for simple tasks, Sonnet 5 at medium effort for judgment, the storyboard at low; the per-model policy lives in `AIService._prepareParams`, shared by `_callClaude` and its streaming twin `_streamClaude`; `cache: true` there turns a long stable system prompt into one cached block, revise and recipe match use it; Sonnet 5 rejects sampling params and prefills); mock mode without API key. **Measured 2026-09-20: no model swap makes Jamyard snappier** (Haiku paths sit at 1 to 2.5 s, writers are bounded by output length, caching changes cost not speed); do not re-run that question without new evidence
- Neon Postgres for persistence (user games/recipes, snapshots, feedback, featured overrides, AI usage); filesystem fallback when `DATABASE_URL` unset
- Deployed on Render; CI deploys on green only
- **License: AGPL-3.0-only** (2026-09-19, `LICENSE` + `README.md` License section; `package.json.license`). The code only: prompt banks carry their own per-prompt credits and no AGPL grant, the Jamyard name and look are the live site's, student work is never in the repo. The home footer links the repository ("source"). A new third-party text or asset gets a credit line in the README's License section, never a silent drop into the tree

## Current Snapshot
- **2553 tests passing** (`npm test`, ~5s; one assertion fails on Windows checkouts only, CRLF) · **321 prompts** across 3 banks (`recipes/prompt-banks/`)

- **31 phase types**, **28 built-in recipes** (one retired), ~35 games in `games/` (**the yard's fifteen** since 2026-09-25, owner's swap: Exit Ticket, Live Poll, Class Critique, Draw Gallery, Folded Pass, Snowball, Solo Quiz, Someone's Got You, Vocab Match, Both Sides of the Rope, One More Thing, Doodle Bluff, Whose Eyes?, Closer, Group Work Day; Speed Quiz, Trivia Bluff, and Estimation Station are built but not in the yard, keeping their examples and pictograms) (varies — use `ls games/`; `_`-prefixed dirs are hidden test fixtures)
- Server on port 3000 (`npm start`); **restart the server after code changes** (no hot reload)
- Full feature history: `docs/CHANGELOG.md` + `docs/CLAUDE-ARCHIVE.md` (detailed ship-log formerly in this file)

## Surfaces & Routes
- `/` home (15b, 2026-09-10, branch `home-15b`; 16h fold on branch `home-16h`, 2026-09-21, FOR VIEWING: the headline "Get the whole class in on it." beside **the picture**, `screens/shared/fold-picture.js` + `.css`, a drawn projector and one student's screen playing Snowball's first minute once on load (Snowball asks "What is the most important idea from this unit?" over a fractions unit since 2026-09-24, owner: class norms read as once a year; the fold's strings in `fold-picture.js` must match the config, a test checks), thirteen states on a dwell table, a Replay button under it, the canvas a fixed 660 by 330 drawing scaled with CSS zoom, `aria-hidden` with a hidden sentence for screen readers, the sub-line and mechanic line gone; round two 2026-09-22 on the same branch: THREE scenes (join and answer, the pair's one shared box, the drawing wall), the carousel GONE, the 13a how-it-works strip flat above the yard, the yard head's subline, headline broken after "class", the question in spaced caps, plank ways on hover; `engine/when-lines.js` holds `when` lines for database-only rows (Rose, Bud, Thorn); the AI door is "Have an idea? Make it real" with a hover card; guarded by `tests/screens/home-16h.test.js`): the fold asks "What does your **class** need?" and answers with four planks (To connect / To think / To review / To just have fun) on a base board, the mechanic line under them; a plank is a filtered jump into **the whole yard** below (every visible activity shortest first as drawn mini-projector prints, the AI door as the last tile), whose chip row is the same control at pocket size on a `position: sticky` head; under the yard, **See one on the projector**: the drawn frame (code, count, timer, first prompt, the template's `glimpse.samples` as answer chips, the pile) beside name/meta/description, three "On their screens" cards, and the page's one red PICK THIS ONE (2026-09-12; it lands on the make page, so it never says "start"). Hover a plank: the label floats up and the ways Jamyard does that job show under it (`.plank-ways`). **Every activity door on the home goes to `/make?game=<id>&from=home`, never `/host`** (owner's call). **Start here** (2026-09-24, PR #71): a card under the how strip for a FIRST-TIME browser only (shipped `hidden`; shown when there is no `jamyard.hostKey`, no copy, no heart, no recent, and `jamyard.startHereDone` is unset; the door's click sets it), the page's one red door "Try Live Poll" to `/make?game=live-poll&from=start` (the make page shows `#start-note`, "press Try it with pretend students", back link home; `start` is an `ENTRY_POINTS` value on both sides of the relay), "or browse the yard" beside it; guarded by `tests/screens/start-here.test.js`. Student join is one yellow tab, code strip on click. Category labels are the four jobs everywhere (`GoalGroups.GROUPS[].job`, `jobOf`); keys stay connect/think/review/play. Guarded by `tests/screens/home-15b.test.js` (the old carousel shots and their regen script left the repo 2026-09-19). **The yard is 17g** (2026-09-22, branch `yard-17g`, FOR VIEWING, `screens/shared/yard-pictograms.js` + `yard-prints.js`): a card is a name, a 10px square in the need's paint, and a pictogram of the mechanic built from blocks in a 156px gesso window on a paper mat (sixteen drawn by id, a copy `exit-ticket-2` reads its template's, anything else a fallback from its glimpse); no prompt, no meta line at rest; on hover the card lifts, the pictogram slides up and its arrival block lands with the prompt under it (keyboard focus too; touch = first tap previews, second opens; the first card plays once on load, never under reduced motion); paper time marks on the first card of each duration step (`marksFor`, steps 5/10/15/20/30, `minutesOf` reads the server's `minutes`); four columns; the head is the name and the 300×44 search, the chip row its own sticky row under it; the AI door keeps "Have an idea? Make it real" with the spec's dashed-slot window; the floating hover card is OFF on the cards (`HOVER_CARD = false`, owner 2026-09-23, code kept); **eight content cards** (2026-09-24, PR #70 MERGED: Live Poll, Draw Gallery, Snowball, Both Sides of the Rope, Whose Eyes?, Closer, Vocab Match, Someone's Got You carry the activity's own words and drawings at rest, `HOVER_LINES` in `yard-pictograms.js` names them and their hover line, the window anchors at the top and never slides, never a caption of ours; names, Maya and Jordan as in the fold picture, say whose idea is whose, the owner's "showing, not telling"; on six of them the text planks are blank at rest and the words roll out on hover, `slip(..., {fill: true})` and `.pg-fill`, the owner's call after seeing it); guarded by `tests/screens/yard-17g.test.js`
- **The home IS the yard** (one yard, 2026-09-13, owner: "there's just one place to view the activities"): under the planks and the carousel, **My yard** (`screens/shared/my-yard.js` + `.css`: your copies, hearts, and recents as the grid's prints on a board, each with tiny tools under its name row (18d, 2026-09-24, branch `my-yard-18d`): play hosts through HostLaunch, pen edits a copy or opens a template on the make page, heart, share (own copies, the popup's link), bin where delete is allowed; **an own copy prints as yours** (18d): a sanded mat, a kicker with the template's name or MADE WITH AI, the copy's topic as a word block on the template's pictogram (`YardPrints.ownDetails`, `topicOf`: the name minus the template's name, else the copy's own prompt's first words, never the template's question), a custom game's initials over the fallback pile; guarded by `tests/screens/my-yard-18d.test.js`; the popup with the doors, Share, the shed; `MyYard.split` is the one-place rule) above **The yard** (the grid of cards, the search beside the yard's name with the chip row sticky under it, a no-match search shows everything with a line). Deep links on the home: `?about=<id>` (popup), `?highlight=<id>` (scroll + flash), `?q=` (search), all stripped after use, `#yard` jumps to the section. **Every "back to the yard" link is `/#yard`.**
- `/library` — the OWNER'S curation console only (search + chips over every activity, cards with every control, ★ Featured toggles), reached through `/owner` (`?owner=1`); the server redirects every other visit to `/#yard` carrying `about`/`highlight`/`q` (`?customize=` goes to `/make`), and library.js sends a cached page the same way
- `/host` projector screen · `/player` student screen · `/teacher` private console (room code + PIN, or SITE_PASSWORD basic auth)
- `/teacher/report` printable activity report (engine/report.js via PIN-gated `GET /api/rooms/:code/report`) — built on demand from live room state, NEVER stored server-side, gone when the room expires; the step that is still OPEN is read live from the players (`liveDataFor`, marked "Still open"), which is how a rolling exit ticket gets read mid-step; browser print dialog = the PDF; names toggle defaults on; console links it (header + end-phase reminder card) **The words about keeping it are one rule** (2026-09-23): printing or saving the PDF keeps the report, a copied link never does, and every surface says so (the projector checklist, Copy report link, the console card, the header link, the guide); the console shows a **Before you project** card once per browser (`#setup-card`, extend not duplicate, which tab to drag, where private answers land; never inside Try it out); the make page says under its doors that a changed activity is a copy in this browser (My yard, Share link for another computer).
- `/make?game=<id>` — **Make it yours as a page** (2026-09-09, `screens/make/`): the first student step as the class will see it (`GET /api/games/:id/print`, `engine/make-print.js`), the question, field labels, and (2026-09-24) a pick-one step's answer chips editable in place (`choicesEditable` from `printFor`, boxes in the chips' paint, the `choices` make edit), a tappable timer on plain configs, one red HOST IT NOW (owner's call 2026-09-16; it was TRY IT), Try it with pretend students beside it, the designer link, and **Make it fit your class** under the doors (2026-09-13, `.fit-section`: rows of chips, nothing folded; every setting is ONE row, the question then the answer as chips tapped in place, a typed answer is the plank in its row; the AI's one or two questions first, then Your class as a chip + "change" link that shows the picker, Student names, Early-bird joke; no tally, no instructions); the AI's questions carry `kind: choice|text` (`AIService.shapeCustomizeQuestion`), choice = chips with "Type your own…", text = the plank with the mic, one question the wording hangs on and a second only if the content depends on it, one for the whole activity never per round, never about timing/rounds/grade/subject; **What happens follows the edits**: the map redraws from the edited copy as the question/labels/timer change (the make route returns `map`), and **See how it reads** runs the AI fit once, redraws from the fitted copy (`POST /api/games/map`), and the doors reuse that copy (`state.fitted`); **a pairs panel** (`pairsFor`, `edits.pairs`) shows and edits every match step's plain-text pairs under the doors (Vocab Match); the designer link reads "Customize it more in the designer" (owner's wording 2026-09-13, the one place "Customize" appears in teacher copy); edits apply through `POST /api/games/:id/make`; untouched = the original runs, no copy. `MakeItYours.open` routes here for every template; a recipe's own editor (quiz, bluff, or setup knobs) mounts under the doors via `MakeItYours.mountPanel` (the print is read-only then, the panel's `makeCopy` saves). The Create page lands here too after a recipe match saves its copy (2026-09-12); a copy that is already yours (`MyGames.has`) saves back to itself with PUT, never a second copy. The old dialog stays behind `window.MAKE_IT_YOURS_DIALOG`
- `/designer` Create page (idea box → recipe match or storyboard; a match shows the server's timing note + "Trim the timers" when it runs over the minutes asked for; **the storyboard streams** (2026-09-20): `POST /api/games/storyboard/stream` sends server-sent events (thinking, name, step, then `done` = the plain route's body) that the plan dialog shows as steps land, `screens/shared/sse-reader.js` + `engine/storyboard-partial.js`; the plain route stays and both share `storyboardOutcome`, so one ideas-log write either way) · `/designer/edit` editor (Simple | Builder | Advanced views; Simple is default; the Settings and Ask AI side panels fold to a rail via `panel-toggles.js`, remembered per browser; Ask AI = the design chat panel beside the Simple view, `screens/designer/chat-panel.js` + `POST /api/games/chat` — proposes changes as cards, Apply gated on validation, one-step Revert; "Just do it" = `forceEdit:true` turn that folds the whole conversation into one proposal)
- `/prototype` ("the simulator", teacher copy "Try it out"; Totem 14a/15a since 2026-09-09) the teacher screen (Class screen | Teacher controls tabs, the real `/teacher` console auto-joined via `#code=&pin=`) beside ONE student screen with a ‹ N of M › pager and a "+ Add another student" slot (the seats come from the activity's shape, `BenchLogic.startingSeats`: a pairing or a merge seats two, teams two groups, else one); the header carries the activity as a name chip and the plan as a row of blocks (click a step: "Skip ahead?" plays the room forward with bots until the live row reaches it; folds to fit, "…" opens the full list; `?goto=<phaseId>` does the same on launch); the yellow NEXT card points at the one control to press now (`screens/prototype/bench-logic.js`, pure + tested; three presses in a row or its × hides it, ? brings it back); a first-visit tour (`screens/prototype/tour.js`, stops in `BenchLogic.TOUR_STOPS`, remembered per browser, replay from the help card) names every piece once the first room is up; "Add sample answers" and "Skip timer" sit under the student screen only while a student step is open and deal the template's `sampleAnswers` by seat
- `/guide` one-page teacher guide (setup, live controls, quick fixes) · `/owner` owner-mode doorway (redirects to the library unlock; no in-page owner links)
- `/feedback` owner inbox (SITE_PASSWORD-gated) · `/rooms` owner rooms log (2026-09-18, `services/room-log.js`, `GET /api/room-log`): one row per real room (activity, code, class or pretend, headcount, step reached of steps, open / ended / closed, minutes, the host page's random `jamyard.hostKey`); the headline is the pilot count, distinct host keys with a class room of 3+ real students since `PILOT_START` (2026-09-18, `?since=` moves it). Never robot playtests. Neon `room_log` or `data/room-log.ndjson`
- `/guide` one-page teacher guide (setup, live controls, quick fixes) · `/privacy` + `/terms` (2026-09-19: plain-language terms, the nine § 49073.1 promises as a numbered list, guarded phrase by phrase in `tests/screens/terms-page.test.js`; linked from the home footer, the privacy page, the guide) · `/owner` owner-mode doorway (redirects to the library unlock; no in-page owner links)
- `/guide` one-page teacher guide (setup, live controls, quick fixes) · `/privacy` is **Privacy and terms**, one page (owner 2026-09-19): the privacy half, then `#terms` (plain-language terms, the nine § 49073.1 promises as a numbered list, guarded phrase by phrase in `tests/screens/terms-page.test.js`); `/terms` redirects to `/privacy#terms`; footer link "privacy & terms". **Never an agree line, checkbox, or consent dialog** (owner: friction and uncertainty; the test checks the Host surfaces) · `/owner` owner-mode doorway (redirects to the library unlock; no in-page owner links)
- `/feedback` owner inbox (SITE_PASSWORD-gated)
- `/feedback` owner inbox (SITE_PASSWORD-gated) · `/ideas` owner ideas log (2026-09-19, `services/idea-log.js`, `GET /api/idea-log`): one row per Create-page try (the idea as typed with contact patterns scrubbed, stage match / alternate / storyboard, result match / existing / none / storyboard / cant-build / error, target, reason, bricks, minutes asked, `jamyard.aid`), `ideaId` rides back to the client and into the `POST /api/games` save so the row learns `saved_game_id`; a new Create-page route or return point must call `logIdea`. Never student text; PostHog keeps only the outcome enum
- `/share/<id>` — share link landing page: "Save to my activities" imports a COPY via `POST /api/games/:id/copy` (engine/share-copy.js; featured stripped, new deduped id, never the same row); built-in ids redirect to `/library?about=`; Share button lives in the yard popup (own activities)
- Vanity URLs: `vanity-urls.json` (slug → game id) mints memorable paths like `/good-question` that redirect to `/host?game=<id>`; server refuses reserved/malformed slugs at startup
- Teacher profile (grade band + subjects, localStorage via `screens/shared/teacher-profile.js`) personalizes prompt-deck picks ("for your class") and Customize; set from **Your class** on the home (2026-09-24, a chip after the four jobs in the yard's sticky row that opens `#class-panel`, the picker from `screens/shared/class-picker.js` + `.css`, moved out of make-it-yours.js and shared with the make page's Your class row) or the make page's "change" link. **Class examples** (`screens/shared/class-examples.js`, AUTHORED, never AI-written, owner's call): with a class set, every yard card shows that activity in the teacher's subject at their grade (`ClassExamples.pick(id, profile, seat)`, subjects dealt round robin across the cards, adult reads the high band, Something else and own copies keep the template's words); the eight content cards redraw their slips from the example's words, the others swap the hover line; Draw Gallery's wall and Doodle Bluff's paper draw the example's drawings by name from `screens/shared/yard-doodles.js` (authored stroke paths in a 60 by 50 box, the fold picture's style; a new drawing is a named entry there, the test checks every name the table uses); the card's link carries `&ex=<subject>.<band>`, the popup's What happens map reads the example through the make route (`ActivityMap.attach(id, holder, {edits})`, `ClassExamples.editsOf`; `applyEdits` takes fields by position and pairs by match step in order for it), and the make page (`applyExample`) puts the same words in the question box, the field labels, the pairs (two rounds of three), or the choices (a new `choices` make edit for a pick-one step), with "Use the original words" under the print; those count as the teacher's own (the fit keeps them, hosting saves a copy). The panel-owned activities (the quizzes, the bluffs, Group Work Day) carry their example as RECIPE PARAMS (`prefill.params`: three checked questions shared by Solo Quiz and Speed Quiz, the same facts as blanks for Trivia Bluff, three phrases with `phraseSource: teacher`, four tasks): the make route compiles the recipe over the stamp when `body.params` arrives (only the recipe's own parameter names) and returns the print too, the make page puts them on the stamp before the panel mounts (the panel then always counts as touched) and draws that print, the map quotes a solo-quiz's questions and a checklist's items as samples. A `swaps` edit (`[{from, to}]`, the make route and `applyEdits`, `SWAP_KEYS` = every teacher-facing text field, never an id or a ref, the sample answers dropped when it hits) carries a topic that lives in more than the first step: the rope's claim, Whose Eyes?'s topic, Closer's first question; `prefill.swaps` on those three, the page sends them with every edit. **Back to home from the make page lands on `/#yard`** (owner 2026-09-24: below the fold, where the activities are). **The picker takes one subject at a time** (owner 2026-09-24; the profile keeps its list shape). Guarded by `tests/screens/class-examples.test.js`; a new featured template needs its rows in the table (the test lists them)
- Surfaces model: **find it in the yard, start it in Create, shape it in the Editor** (docs/SURFACES-PLAN.md). Every Make it yours dialog ends in the shared doors (`screens/shared/make-it-yours-doors.js`, 2026-09-07): a smaller Continue setup in the designer beside a bigger Launch, which opens Try it out with pretend students / Host it now (untouched = the original runs, no copy saved)

## Standing Rules (active doctrine — check before writing code or copy)
- **No em dashes in user-facing text** (students read them as an AI tell); comma, colon, or new sentence instead. En dashes in numeric ranges fine; Along bank verbatim; code comments/docs exempt. Enforced by `tests/style/no-em-dash.test.js` + `STYLE_RULES` in `AIService._callClaude`.
- **No decorative emojis** in descriptions/messages/prompts; icons only in icon slots (functional marks like ♥, goal chips, avatars, medals are fine).
- **Teacher-facing helper copy names what is on the screen, never our words for it** (owner, 2026-09-16, four rounds on the make page): a teacher reads "Either door fits the wording" and asks "what door?"; "Runs the AI fit once" means nothing to them. Say the button they can see ("Host it or try it, and the wording gets fitted to your class"), say what changes and where ("Rewords the screen above and What happens below"), and drop the timing unless it changes what they do. "Door", "fit", "AI fit", "print", "plank", "knob", "brick" are code and doc words only.
- **Nothing a student or the projector reads names the AI** (owner, 2026-09-16: "there is an increased backlash against AI so I would like to remind them as little as possible"). The wait screen names the work ("Putting the answers together..."), the Continue label is "Put it together", the answer-box footer is "These get summed up for the class.", a failed call reads "Something went wrong putting the answers together", mock mode says "(Mock)". Teacher surfaces (console, make page, designer, guide, privacy copy) keep their AI wording; Human vs AI and Mad Lib Mashup keep theirs because the AI is the fun. Guarded by `tests/screens/no-ai-mention.test.js` (string literals on player.js, host.js, the i18n tables, continue-labels, audience; the two HTML shells; every built-in's student text). **AI prose gets a scrub on the way to the wall**: `STYLE_RULES` in the AI service (one chokepoint, every call) now carries the owner's list of tells (no dashes, contractions, no "not X, it's Y", no rule-of-three habit, no announcing or summarizing openers, no cursed vocabulary), and `RichText.scrub` (`screens/shared/rich-text.js`, run by `parse`/`buildBody`, never by `applyInline`, so prompts and the Along bank show as written) turns dashes into commas, keeps digit ranges, and drops "It's worth noting", "Overall," and their kin. Never a second model pass on game-time text.
- **Teacher-facing vocabulary (owner's calls, 2026-09-02, revised 2026-09-07):** the /library page is **the yard**; the copy-and-tailor action is **Make it yours** (never "Customize", except the make page's designer link, "Customize it more in the designer", the owner's wording 2026-09-13); the /prototype page and its button are **Try it out**, qualified "with pretend students" where there is room (never "Simulate" or "Preview" in teacher copy; "Teacher preview" the step type and the editor's live-mockup toggle are different things; "practice players" is out too, they are pretend students). "library", "customize", "preview", "simulator", "sim" survive only in routes, ids, class names, file names, function names, script names, and code comments. Same split as activity/game.
- **"Activity" vocabulary** in user-facing copy ("game" only when it truly is one). Internals keep "game" (`games/`, `gameId`, socket events, API routes) — never rename them.
- **Host opens two tabs** (2026-09-12, `screens/shared/host-launch.js`): the projector in a NEW tab (it takes focus, so the teacher stays on the host view), the teacher console in the tab Host was pressed in, paired by a nonce (a blank tab opened INSIDE the click, sent to the projector by `hostUrl()`, the room published to the nonce on `room-created` via localStorage + BroadcastChannel). Every Host door calls `HostLaunch.begin()` in the click (or `launch()`), gets its address from `HostLaunch.hostUrl()`, and `abandon()`s on a failed save; a new Host button must do the same, and its page must load the module (guarded by `tests/screens/host-launch.test.js`). **The arrangement cannot be flipped** (owner asked 2026-09-13, probed in a headed Edge over CDP): a page cannot open a tab BEHIND the current one (Chromium turns a synthesized ctrl-click into a foreground tab) and the opener cannot take focus back (`window.focus()` sync or delayed, `w.blur()`, the child's `opener.focus()` all left the new tab in front). Do not try again without a browser extension.
- **The screen that is up fits the viewport, no scrolling** (owner 2026-09-18, "most projectors fit 16:9"): `screens/shared/fit-screen.js` on the projector and the student screen shrinks the active section with CSS zoom until the document fits (floor 0.5 on the projector, 0.6 on the student screen, then it scrolls), re-fitting on every mutation, resize, font, and image; both `showSection`s call `FitScreen.fitNow()`. A new in-flow element outside a section (the early-bird joke is one) must be added to the player's `targets`; a new screen that a class looks at loads the module before its own script (guarded by `tests/screens/fit-screen.test.js`). Measure before and after with `node scripts/audit-fit.js <outDir> <game...>` (its own server on :3005; the summary lists every step that overflows at 1920x1080 and 1366x768).
- **The host screen is a projector** — never put teacher-private info there; that's what `/teacher` is for.
- **Payoff beats are host-paced, never timed** (reveals, winners, galleries).
- **Rolling start is a family, not a knob** (`start: "rolling"`, engine/phases/rolling.js): the room opens straight into the first step, the doorway card stays on the projector, timers are ignored, and each student gets their own done screen. Only for activities without roster-bound steps (pairs, teams, chains, rounds; the validator warns). Exit Ticket, Live Poll, Solo Quiz are the three shapes.
- **Fixed UI labels go through `engine/i18n/`** (server labels via `continueLabelForPhase(phase, phases, lang)`, screens via `UiLang.t('Submit')` + `UiLang.apply()`); a new student- or projector-facing label needs a row in every language table (drift-guarded). Top-level `language` (auto | en | es | fr | de | pt | it) resolves once in the GameEngine constructor; auto = stopword detection over the activity's own text.
- **Nothing student-drawn reaches the projector without a teacher gate** (preview phase or moderation).
- **The AI never fakes fresh facts** (2026-09-10): every prompt that writes questions or facts carries `FRESH_FACTS_RULE` (services/ai-service.js) and an honest escape (`needsTeacherFacts` for the quiz/bluff writers, `cantBuild` for the storyboard, `noMatch` for the matcher). A new fact-writing prompt must carry the rule and its caller must read the escape; the teacher is told to paste the facts or write the questions.
- **No student name in any outbound AI payload** (pseudonymous playerIds + `engine/ai-name-fill.js` re-fill; `engine/pii-scrub.js` on free text — pass `rosterNames` on game-time calls).
- **Student text, teacher config, and AI output are all untrusted for rendering** — prefer `textContent`; HTML interpolation must escape (`escapeHtml`/`escapeHtmlText`; enforced by `tests/screens/xss-sinks.test.js`).
- **Teacher-save purity (§49073.1)** — saved objects (configs, recipes, templates) never contain student-generated content; anything that would needs full student-data treatment — prefer never taking it.
- **Projector style** (docs/PROJECTOR-STYLE.md) — content owns the projector; brand shrinks to a corner mark via `body.in-activity`.
- **Analytics is a relay with an allowlist, never a vendor script** (2026-09-13, `services/analytics.js` + `screens/shared/analytics.js`): no page loads PostHog; teacher pages post named events to `POST /api/track`, the server sanitizes against `ANALYTICS_EVENTS` and batches to PostHog with no person profiles and no geo. The student screen, the projector, and the console load nothing (guarded by `tests/screens/analytics-surface.test.js`). A new event = a row in `ANALYTICS_EVENTS` (route / enum / int / bool / slug kinds, plus the URL-derived host and tag kinds for referrer and utm_*; never free text) + a line in the privacy page's usage-events row; only `/api/track` passes `extra.ip` (country and region for teacher-page events, PostHog discards the address: the project's "Discard client IP data" setting is load-bearing) and `extra.host`; on the wire `page_viewed`/`page_left` become PostHog's own `$pageview`/`$pageleave` with `$current_url`/`$pathname`/`$host`/`$referrer`/`$referring_domain`/`$session_id` (`toPostHog`), which is what its dashboards count, and every event carries the browser's per-tab UUIDv7 visit id (`Analytics.sessionId()`, shared with the replay recorder); `Analytics.track('name', {...})` in a screen must name a listed event (the test checks). Server events ride a random per-room id, never the code; robot playtests send nothing. Every room event carries `kind` (class | pretend, from `pretend: true` on the create-room payload, which the host page sets under `?prototype=true`), and `room_abandoned` (step of steps, headcount, minutes) fires when a room closes without reaching its end (2026-09-18); the rooms log at `/rooms` is the per-room view of the same rooms and lives in our own DB, never PostHog. **Session replay is the one PostHog script in a browser** (`screens/shared/replay.js`, 2026-09-13): six authoring pages only (`/`, `/library`, `/make`, `/designer`, `/designer/edit`, `/guide`), never a page a student or the projector loads, never the console, Try it out, or inside a frame (`tests/screens/replay-surface.test.js`); recording is the SDK's only feature (no autocapture, no page views, no cookies), inputs and teacher-typed text masked, iframes blocked. A new teacher page that shows student content must NOT load it; a new teacher-typed surface gets `.ph-mask`. `POSTHOG_REPLAY=0` turns it off.

## Gotchas & Patterns
- **Foreach sub-phases run in key order, and JSONB scrambles key order** (2026-09-06): `user_games.config` and `user_recipes.recipe` are JSON (never JSONB; jsonb sorts keys by length so Doodle Bluff copies voted before the fakes). Old rows are repaired on read from their recipe stamp (`repairSavedConfig` → `engine/subphase-order.js`); AI edits carry the order (`carrySubPhaseOrder`); the validator warns on a sub-phase reading a later sibling. Any new JSON column that holds a config must be JSON.
- **A late joiner is seated, never stranded** (2026-09-14, `engine/phases/late-seating.js`): a FRESH join (never a reconnect) that lands in an open team step goes through the handler's `onLateJoin(ctx, playerId)` before `sendCurrentState`, so the newcomer's first screen is their seat. team-split: still choosing = eligible plus a spot (the emptiest team's cap grows by one, caps always sum to the eligible count), being arranged = in the console's unplaced list, teams set = the smallest team, written into the split's stored `{teams, playerTeam}` so later steps and the report see it. team-roles: a seat in the split's data first, then the group's menu while open, the group's least-held role once set (`seatInRoleOutput` also updates `rolesList` and `roleMembers`). checklist: the team's list (a fresh solo list without teams), a role when the step has `rolesFrom`; pair-born groups get no seat. The consoles get `teacher-late-seat` (one line, "Maya joined late: on Group 2 as Recorder"). A new roster-bound handler should add `onLateJoin` returning `{team, role, picking}` or null, and refresh the projector itself. Proof: `scripts/simulate-late-seating.js` (19 checks over both fixtures).
- **The host screen rejoins on EVERY socket reconnect, whatever its URL says** (2026-09-10, `screens/host/host-session.js`): the `?game=` / prototype "fresh room" rule is for page loads only. A reconnect (wifi blip, the projector tab put to sleep behind the teacher console) gets a new socket id, and without `host-rejoin` the server has no host: students land on the console and never on the projector, and the room closes after 5 min. The console shows "projector not connected" from `hostConnected` on `teacher-roster`. Any new surface that binds to a room by socket id must rejoin from memory on 'connect' (the console already does).
- **The games list is scoped to the visitor** (2026-09-20, `engine/games-list-scope.js`): `GET /api/games?mine=<own ids>` returns built-ins, featured user rows (override or saved `featured: true`), and the listed ids, plus `ids` (every id on the server, for the copy-id dedupe); no `mine` = everything (the owner's library console only). A new page that lists activities passes `MyGames.list()` as `mine` and reads `ids` for dedupe, never the bare list (96 other teachers' rows, 60 KB). **Class pages load `/socket.io/socket.io.min.js` and connect websocket first** with polling as the fallback; **every page preloads the two fonts before `totem.css`**. Guarded by `tests/screens/snappy-pass.test.js`; measured with the scratchpad CDP timing script, cold loads on school wifi were the number that mattered (1.0 to 2.7 s).
- **CSS display value overrides the `hidden` attribute** — any styled-display element toggled via `.hidden` needs a `[hidden]{display:none}` override in the same stylesheet (bitten 3+ times).
- **A forward press on an open answer step is a Close, never a skip** (2026-09-18, owner played Doodle Bluff from the console and it "ended early"): `advance-phase` on an open collect / collect-choice runs `closeCollect` (the lifted close-submissions body, idempotent via `phaseState.closingCollect`), so the answers are stored before the room moves; the console hides its second forward button while an answer step is open. Skipping the gather leaves the next step reading nothing (Doodle Bluff: zero drawings, zero rounds, straight to the end). Proof: `scripts/simulate-console-skip.js`. A new answer-style step type must be added to that guard.
- **Close/* handlers must kind-guard `room.phaseState` AND be idempotent** — all-inputs-in auto-advance races a late host click.
- **A Close must wait for in-flight submissions** (`engine/pending-submits.js`, 2026-09-04): submit-response awaits the moderation ladder, so a teacher's Close can land while the last answers are mid-flight; close-submissions settles the holds first, then re-checks the phase. Any new async gap in a submit handler takes a hold; any new gather-and-advance path settles them. (Doodle Bluff's "only one title to pick" was a rotation dealt from a short list.)
- **Multiple-choice ballots are ONE list per step** (`buildSharedBallot` in collect-choice.js): sample + shuffle once, stored on `room.phaseState.ballot`; a student's own fake is removed from their copy only. Never shuffle per player.
- **New gameplay socket handlers**: `checkEventPayload` + `isStalePhaseEvent` + `recordEvent`; every handler runs in a try/catch wrapper.
- **New phaseState containing player ids** gets reconnect id-migration free via the deep walker (`engine/id-migration.js`); **new closers** must be reachable from the `advance-phase` routing switch.
- **New transition-bearing fields wire in SIX places**: state-machine graph, validator ref-existence, BFS+cycle (server AND client), recipe drop-rewiring, editor delete-relink.
- **Player-facing payloads sent from a handler's `onReconnect` must carry `phaseInstanceId: ctx.phaseInstanceId`** (direct `socket.emit` skips the `withPhaseSeq` wrapper); late joiners are the normal case in rolling activities, and a missing id makes the client's stale guard drop every follow-up (solo-quiz feedback, 2026-09-02).
- **Word help is a ledger plus a lookup** (2026-09-06, `engine/word-help.js`): top-level `wordHelp: {tokens, to}` gives each student a purse of word translations; the server owns the count (`room.wordHelp`, id-migrated on reconnect, snapshotted on restart), the client only displays it. `screens/shared/word-help.js` wraps every `setRichText` sink; new prompt sinks get tappable words for free, other text (choices, buttons) does not. Any future rationed help (hints, examples) should spend from the same purse rather than invent a second one. The tapped-word log is counts only, teacher surfaces only.
- **The editor always autosaves, no Save button** (owner 2026-09-24): the first real edit saves a new activity under a fresh id; `saveGame` stays for Host it now. **The joke row reads "Dad joke for the first students to join"** on the make page and in the editor's Settings (owner: "Early-bird joke" was vague); the setting's name in code stays `earlyJoke`.
- **Top-level teacher settings follow one wiring** (anonymous, language, wordHelp, earlyJoke): validate in `engine/game-loader.js` + mirror in editor.js `validateConfig`, a Settings row in editor.html/editor.js (render + read), a `carryX` in server.js at BOTH AI revise/chat call sites, room state created in `create-room` and id-migrated in `join-room`'s reconnect branch, serialized/restored in `engine/room-snapshot.js`, and the /make More fold if a teacher would want it at Make time. **Early-bird joke** (engine/early-joke.js, 2026-09-12) is ON BY DEFAULT: absent = first 10, `earlyJoke: false` = off, `{first: N}` = the count; the server deals by NEW join order and re-sends the same joke on reconnect; the card folds away by itself when the room moves to a later step (`earlyJokePhaseSeen`, player.js: the phase id first seen within two seconds of showing it is home, any other id hides it); the list is `engine/dad-jokes.json`, rebuilt from `docs/500-all-ages-dad-jokes.md` by `scripts/build-dad-jokes.js` (test fails when stale).
- **Bold is `**word**` and nothing else** (2026-09-03): the one inline format in teacher text. Simple view edits it as real bold (`screens/shared/bold-box.js`, contenteditable with a `.value` accessor; base weight pinned to 400 or the browser's bold command toggles off); every prompt/instruction sink on host and player goes through `setRichText` (`RichText.applyInline`), where bold paints yellow (`.prompt-bold`) because prompts are already heavy. New text sinks must use it, never bare textContent; excerpts strip the markers.
- **Multi-field collect responses nest under `fields.*`** — reveal templates reading top-level keys render silently blank.
- **AI responses wrap JSON in preamble** — always regex-fallback extraction.
- **Every Claude reply is read through `extractText(message)`, never `content[0].text`** (2026-09-07): Sonnet 5 replies open with a thinking block, so the old shape is undefined; the storyboard door was dead for four days that way. Mock replies in tests should lead with a thinking block when they guard a Sonnet caller.
- **Storyboard bricks** (`screens/shared/step-suggestions.js` + `engine/suggest-validate.js` + the storyboard and concierge prompts in ai-service.js, all four must agree): announce, collect, collect-two, collect-choice, estimate, reveal, reveal-one, vote (`approve: true` since 2026-09-25 = yes or no on every answer, the compiler appends a reveal of `{{vote.approvedList}}` instead of the crown; the chat and revise prompts teach the same mode and that a vote is always followed by the step that shows its result), guessing-rounds (`guess:"who"` = the Who Said It? roster shape), rank (rank step + host-paced reveal of the order; `items` for a teacher list; `byGroup: true` after a teams step = each group's order, its members' average), assign (Hand out choices: needs the rank right before it, every group or student gets one item, first choices first, `perChoice` caps a spot; a rank followed by assign with a teams step earlier ranks as groups on its own and skips the class-order reveal), quiz, teams, chain (pass-around), deal (Story Ingredients' shuffle-and-deal: 2-4 `piles`, a writing step over the hand, share-out built in), pairs (2026-09-20, the debate-pairs shape: `text` = what partners write first, `rounds` = 0-3 follow-ups with the SAME partner and the partner's latest piece under the instruction, `sides` = two labels dealt one per partner; the AI writes plain `{{side}}` / `{{otherSide}}` / `{{partner}}` and the compiler binds them to the step; a pair-private reveal of the last exchange closes it, nothing reaches the projector; proof `scripts/simulate-debate-pairs.js`, 18 checks over a pair and a triple), roles (2026-09-20: a job for every member of the last teams or pairs step via team-roles, `roles` 2-8 names, `method` random | choice, optional `tasks` = a checklist with `rolesFrom`, "Job: task" tags a line; the prompt forbids rank + assign for jobs, which hands ONE item per group), end; the estimate brick also takes `answer` + `unit` + `scoring` closest | graduated (2026-09-20: with an answer the step scores the closest guess, without one it is a poll and the prompt forbids promising a winner; the AI only writes a number the teacher named or it is certain of); draw (2026-09-20, Draw Gallery's shape: a drawing collect, the teacher preview gate with reject = redraw, a reveal-one gallery from the drawings; `text` = what to draw, `gallery` = the line over the wall, `timer` default 90; a vote fed by a drawing step is refused with a problem since a ballot shows only text, the prompt keeps a favorite to a show of hands), summarize (2026-09-20: the Builder's AI pair from words, `buildAiPair` with task summarize over the last collect and the reveal of `{{ai.result}}`; `text` = how to sum up, `heading` = the projector line, which never names the AI); **collect takes `items`** (2026-09-24: a teacher list dealt one per student in private, `dealItems` + `{{thisStep.assigned}}` bound to the step's id; THE brick for "each student secretly gets a state", assign is public); announce, collect, and collect-choice bricks also take `video` (2026-09-20: a YouTube link the projector plays, the compiler mirrors `engine/video.js`'s five link shapes and drops anything else with a problem). A new brick needs a golden prompt in `tests/designer/golden-prompts.json` and a line in the matcher prompt. **A vote's `.winner` is an id, `.winnerText` the words** (2026-09-24): the chip and the AI's field list say so, the compiler rewrites `.winner}}` in step words, the validator warns (`VOTE_WINNER_ID_IN_TEMPLATE`). **The matcher sees every recipe's and activity's step list** (`recipeStepTypes`, a legend) and answers `missing` (what the idea asked for that the steps lack, shown on the card); the ready-made list is featured built-ins only. **The design chat card shows a computed diff** (`screens/shared/config-diff.js`), never only the AI's summary.
- **Timing claims come from `engine/duration-estimate.js`, never the AI** (2026-09-06): the matcher is told not to claim a fit; the route computes the estimate from timers plus named allowances and attaches a trimmed copy when it runs over the minutes the teacher named. New allowances or floors go in that module's tables.
- **A Create-page idea lands on the recipe, never on a recipe-born built-in's defaults** (2026-09-23, a reviewer's "five-minute anonymous history poll" with four choices opened Live Poll's default question, names Shown): the matcher's ready-made list tags recipe-born built-ins (`[built from the "live-poll" recipe with its default content]`, from the `recipe` field the route adds to `matchGames`), the prompt asks for THAT RECIPE whenever the idea carries its own question, choices, topic, or list, and the route refits a recipe-born `game` pick to its recipe with a forced match anyway (`engine/match-refit.js`, `refitRecipeIdFor`; one extra Haiku call only then, the first pass's alternates kept); a teacher's own answer choices are used all, in their order, never "3-5 sensible options" over them. **Top-level settings an idea names in words are read by the server, never the AI** (`engine/idea-settings.js`: `parseAnonymity`, "anonymous" / "no names" / "names hidden" = names off, "not anonymous" / "with names" = on, silence = the recipe's own), written by `applyIdeaSettings` onto the compiled config BEFORE the timing report (so a trimmed copy carries them) and returned as `settings` for the match dialog's "Student names" row; a new setting a teacher might name in an idea goes through that function. The "already exists" card (a hand-made built-in) now says what you typed is not copied into it. Guards: `tests/engine/idea-settings.test.js`, `tests/engine/match-refit.test.js`, the recipe-born block in `tests/services/match-recipe-existing-game.test.js`, `tests/screens/create-carries-idea.test.js`, golden prompt `history-poll-anonymous`.
- **A make-page question edit swaps the old wording everywhere** (2026-09-24, `swapWords` in `engine/make-print.js`): a recipe can quote `${question}` as words in a later step (Live Poll's results template), so the `prompt` edit walks every SWAP_KEYS field and the recipe stamp's params with the old text; a new recipe that repeats a param as words gets this for free, a new edit kind that changes a repeated phrase should call the same walker. **A refreshed student screen rejoins its seat** (`playerRoom` in sessionStorage beside the token, never in Try it out). **A dropped Close answers `close-ignored`** with its reason; the host retries once on `not-host` after a rejoin. Proof: `scripts/simulate-ready-made-trust.js`.
- **A student screen says "submitted" only after `response-accepted`** (2026-09-06): every submit path in player.js goes through `awaitSubmitAck`; new collect-style inputs must too, and any new server store path must emit the ack.
- **Identical generate instructions = identical output across sessions** — ai-process appends a variety spin (random seed + inspiration word + chestnut ban, `engine/phases/variety-spin.js`) to every `generate` task at call time; never bake session randomness into recipes/configs (drift guards).
- Phase handlers self-register in `engine/phase-handlers/` via `registerHandler(type, {onEnter, onReconnect})`; use `EVENTS` constants (`engine/events.js`) and `players.listPublic()` (strips tokens).
- Review prompts in `services/ai-service.js` (PHASE_EXTRA_GUIDANCE) must be updated when adding phase types.
- Client validation in `screens/designer/editor.js` mirrors server validation in `engine/game-loader.js` — change both.
- **Make it yours is one shared flow** (2026-09-07, `screens/shared/make-it-yours.js` + `.css`): the yard's cards, its `?customize=` deep link, and the home popup all call `MakeItYours.open(game, btn)`; the dialog never lives in a page script again, and a page that adds it loads dialog, activity-prefs, my-games, teacher-profile, setup-knobs, growing-text, speech-input, and make-it-yours-doors first (guarded by `tests/screens/make-it-yours-module.test.js`).
- **Step display names live in `screens/shared/phase-names.js`** (one canonical name per phase type; palette, Builder cards, refs, pickers, and the storyboard all read it). New phase types must be added there; never hardcode a step name in a screen.
- **Foreach rounds: the people who know the answer sit out** (`engine/phases/sit-out.js`, 2026-09-06): the item's author AND the author of what it was made from (`_current.assignedFromId`, stamped by close-submissions on rotated collects) are stamped onto each collect/collect-choice sub-phase as `_foreachSitOutIds`; every count (enter, emit, reconnect, submit "N of M", close gather, teacher list) reads them through `withoutSitOut`/`sitOutMessage`. Never filter on `_foreachAuthorId` directly again. Anyone handed the SAME item as the round's author sits out too (a short teacher list repeats). `collect.dealItems` hands out a teacher list one per player (`{{thisStep.assigned}}`), the no-author alternative to `rotateFrom`. A `limit`ed foreach keeps the unsampled items as `{{X.skipped}}` (a reveal-one gallery of the drawings that got no round); a reveal-one with nothing to show skips itself.
- **The answer box says who will read it, computed, never configured** (2026-09-07, `engine/audience.js`): collect and collect-choice send `audience` (+ `nextHint`) in `game-started` via `engine/phases/audience-line.js`, read off the graph (rotation/merge/pair/return-to-author = a classmate; projector-facing = the class, "after your teacher reviews it" when a preview gate is on the path; both = combined; AI only; nothing = teacher only; anonymous appends "Names are hidden."). A new consumer type or hand-off field must be classified there (and the label rows live in every i18n table). The classmate line counts heads (2026-09-23): `audienceFor` returns `groupSize` (2 for a pairing or a merge of two, 3 or 4 for a bigger merge, null for a rotation or a chain home) and `labelForGroup(audience, playerCount)` says "One or two classmates" when a pairing does not split the room and "A few classmates" for a merge of three or more; `audienceLine` takes `{playerCount}`, every collect payload passes the head count, six rows per language table. The console's Live entries hint reads the same key (`audience` on `teacher-phase` and the join snapshot, `ENTRIES_HINTS` in teacher.js): a private step never mentions a reveal or the AI. Own-scope reveals take `chainHeading`/`chainGrewHeading` (Someone's Got You's "Someone wrote this for you:"), honor `content` as the projector line, and send `ownReveal` so the student screen drops "The Result:".
- **A vote can be over drawings, and a storyboard vote always ends in a crown** (2026-09-20): a drawing collect's responses reach the vote ballot as thin strokes (`thumbnailStrokes`, `engine/drawing.js`: 24 points a stroke, 600 a drawing), the student ballot draws them (`fillVoteButton`, player.js), `findWinnerEntries` keeps a drawing entry with its strokes, and the projector draws the winning entry under the crown. The storyboard's vote brick sets `excludeAuthors` and appends a `winner` step whenever the vote is over the class's answers or drawings (never over literal options; that builder uses `question`, the vote step's field). Proof: `scripts/simulate-drawing-vote.js` (20 checks).
- **A guessing step's answer can come from the console** (2026-09-20, the jar count): while an estimate step is open the console shows "The answer" (a number box + Set the answer); `estimate-set-answer` (teacher-only, open step only, stale-guarded, `engine/event-schemas.js`) writes `room.phaseState.answer`, every console hears `teacher-estimate-answer`, and the existing close scores the closest guess and reveals it. The console learns the standing answer through `estimateAnswer` on `teacher-phase` and the join snapshot (`estimateAnswerFor`). Nothing reaches students before the close. The storyboard's estimate brick therefore keeps `unit` and `scoring` even without an answer. Proof: `scripts/simulate-estimate-answer.js` (10 checks: set before close scores, a student's event is ignored, after the close it is too late).
- **Spotlight: the teacher puts ONE student's finished work on the projector** (2026-09-25, `engine/spotlight.js`, a third reviewer's water-cycle chains never left the students' screens): the console sends `spotlight {code, playerId}` and the server reads the words (or the strokes) out of the room's data (`spotlightItemFor`: the finished chain during a return-to-author reveal, the student's answer while a collect step is open; hidden entries and passes never), then `spotlight-show` to the host paints a paper card ("Take a look", the name small) that the next Show replaces and the next step clears. The console's Live entries carry a Show button (not when the audience key is `teacher`), and a return-to-author reveal lists its **Finished chains** (`teacher-chains` from the reveal handler after it stores them, `chains` on the join snapshot; the `teacher-phase` event fires BEFORE onEnter, so the list rides its own event). **A return-to-author reveal stores every finished chain** as `responses` (`{playerId, name, text}`, one per starter, `buildChainRecords`) plus `chainList`; the reveal schema's output is dynamic (`revealOutput`, only for `scope: "own"`), so `{{poem.responses}}` feeds a reveal-one gallery (Exquisite Corpse's `share` step), a vote, or a rate step, and the report gives that reveal a section ("What each one became, start to finish"). A steps chain joins its hops with arrows, a template chain fills the sentence, a final chain keeps the last hop. **Validator `PAYOFF_TIMED`**: an announce, reveal, or reveal-one with a `discussionPrompt` AND a timer warns (the class moves on before the question can go up). **A retired recipe** (`retired: true`, `recipes/group-work.json`) stays loadable by id for the copies stamped with it but never lists in the picker. Proof: `scripts/simulate-show-the-chains.js` (20 checks).
- **A discussion prompt is the teacher's, until they show it** (2026-09-07): `discussionPrompt` is a screenControl field on any top-level step; the console shows it ("Something to ask") and `show-discussion` asks the server to put THE STEP'S OWN text on the projector (`discussion-prompt` to the host, a paper card that clears on the next step). Never accept prompt text from the client. Quiz-show items carry `explanation` + `discussionPrompt` (defaults "", filled by `applyDefaults` for object items, since a missing `${item.x}` throws in the compiler).
- **A collect with two or more labelled fields asks its questions in the glimpse** (2026-09-24, `fieldQuestions` in `engine/home-glimpse.js`): Exit Ticket's card reads its two labels, never the instruction in its prompt; one field keeps the prompt. **Sample sets carry a subject on purpose**: Exit Ticket photosynthesis, One More Thing the causes of World War I (2026-09-24), Snowball a fractions unit, Vocab Match literary terms; a new featured template's set should read as a lesson, not an icebreaker, unless the activity is one.
- **Sample answers are template content, dealt by seat** (2026-09-07, `engine/sample-answers.js`): top-level `sampleAnswers` maps a collect step id to lines (string, or array per field); `{respondsTo, lines}` answers an earlier step's line j and the picker (`pickSampleAnswer`, `screens/shared/bot-brain.js`) matches whichever line is on the student's screen. Try it out's "Add sample answers" fetches the config and posts `{samples, seat}`; the collect `game-started` payload carries `phaseId` for it. A featured template with a text collect step MUST carry a set (guarded by `tests/engine/sample-answers.test.js`); recipe-born built-ins put it in the recipe template too (`$if` it when the step is conditional). A TEMPLATE's set is authored, never AI-written; **a teacher's own activity gets a written set** (owner 2026-09-24): `POST /api/games/:id/sample-answers` (`AIService.writeSampleAnswers`, Haiku, one call, saved on the copy, built-ins refused) when Try it out finds none. Never student text.
- **Recipe-born configs carry a provenance stamp** (`config.recipe = {id, version, params}`, written by `compileRecipe`) that powers the library Customize setup knobs. `games/speed-quiz`, `games/trivia-bluff`, `games/exquisite-corpse` (Folded Pass since 2026-09-25), `games/doodle-bluff`, `games/group-work-day` AND `games/estimation-station` (2026-09-25) (2026-09-13: its tasks are plain lines, a "Job: ..." prefix tags the task at game time) are drift-guarded: their phases must deep-equal a fresh compile of their stamp — change the recipe or the stamped params, never hand-edit their phases.
- **Setup knobs come in seven kinds, every one a ROW of chips** (`screens/shared/setup-knobs.js`, rendered by the knob block in `make-it-yours.js`, styled `.knob-*` in `make-it-yours.css`; 2026-09-14, owner: "the bar doesn't need to take up the whole width, it could also be multiple choice"): scalar `setup: true` (integer/boolean/enum; a range of ten or fewer values is number chips, more is a number box; a scalar that must hide behind another knob uses the object form without a mode, `setup: {showWhen}`), array `{mode:"count"}` (how many), array `{mode:"lines"}` (a textarea), array `{mode:"tags", suggestions:[...]}` (names as chips, "Type your own…" adds one, `maxItems` is the cap), array `{mode:"list", tagFrom:"<tags/lines param>", tagLabel?}` (one growing box per item with an ×, "+ Add one", Enter adds the next; with `tagFrom` each box gets a picker that tags the item as a `Name: text` prefix, `SetupKnobs.splitTag`/`joinTag`, the same rule the checklist reads at game time; the pickers vanish and items save untagged while the source knob is hidden), string `{mode:"text"}`; any object form may carry `showWhen: "otherParam=value"` (or `"otherParam=a|b"`). An enum or boolean may carry `valueLabels` (the chip text per value) and `valueHelp` (a helper line per value, the panel shows the picked one; a boolean's keys are `"true"`/`"false"`). Group Work Day is the model: a jobs switch (`withRoles`, `$if` drops the roles step and `rolesFrom`), the jobs as suggested names, the tasks as a list with a job picker. A text knob may carry `setup.writes: {list, count?, button?, then?}`: a button (and a first-time auto-run when the list is empty or still the template's) that asks the AI to write `count` items on the topic into the `list` knob via `POST /api/games/phrase-list`, and `then` = params set at compile once the list has content (Doodle Bluff flips to `phraseSource: "teacher"` so the room deals the written list). Validated in `engine/recipe-schema.js`; rendered as `.knob-row`s by `MakeItYours.mountPanel` on the make page.
- **`$repeat`/`$map` count mode**: `forEach`/`$map` over an INTEGER recipe param iterates 1..N (`${item}` = round number) — for "how many rounds" knobs where round content is generated at game time (trivia-bluff). Customize's AI interview receives the dialog's knob labels (`knownSettings`) and must never re-ask them.

## 31 Phase Types
`engine/phase-schemas.js` is the single source of truth (validator + AI prompts + editor fields + `{{...}}` grammar). Quick reference:
1. `lobby` — wait for players
2. `collect` — text/drawing input; `rotateFrom` (rotation chains + `assignedFrom` links; `rotateShuffle:true` = random no-self deal instead of fixed shift, chain per-pool for multi-pool deals; leave `{{X.assigned}}` out of the prompt = a BLIND hand-off), `prefillFromAssigned`, `appendOnly`, `showTail:N` (the exquisite-corpse fold: player sees only the last N words of the inherited text, full text still accumulates; requires appendOnly), `maxLength`, `assign:"pairwise"` (+`oddHandling:"triple"`, `rotatePairsFrom`, `reusePairsFrom` — accepts a pairwise collect OR team-split, `pairBy:{from,mode}` answer-keyed pairing from a collect-choice — opposite/same, best-effort), `passAllowed`, `simultaneousReveal`, `inputType:"drawing"`, `sides:["For","Against"]` (2026-09-20, with pairwise: dealt one per partner, a triple gets two of the first; per-player tokens `{{X.side}}`, `{{X.partnerSide}}`, and `{{X.partner}}` = what the partner wrote at pairwise step X, for a later step that `reusePairsFrom` it; `engine/per-player-template.js`, a missing partner reads an i18n line)
3. `ai-process` — AI processes data; `perPlayer:true` for one item per student
4. `vote` — head-to-head, pick-one, or **approve** (2026-09-25: yes or no on EVERY entry, several pass at once; `passAt` 1-100 = the percent of yes votes an entry needs, default more yes than no; outputs `approvedList` (numbered, with counts, THE one for a reveal), `resultsList`, `approved`, `rejected`, `approvedCount`, `noCounts`, `scores` = yes counts; the compiler's `approve: true` vote brick appends a reveal of it instead of a crown; the validator warns `VOTE_RESULT_UNREAD` on any vote no later step reads; proof `scripts/simulate-approve-vote.js`); `matchupsFromPairs`/`excludeAuthors` (pick-one and approve too since 2026-09-10: own answer off the ballot, server refuses a self-vote), literal `candidates`, `nextByWinner` branch routing
5. `eliminate` — remove players by percent or hook; `untilRemaining` (with loopBack) ends the rounds once that few remain, so rounds follow class size; a full tie eliminates nobody
6. `reveal` — show content; `scope:"pair"`+`pairsFrom` (pair-private), `scope:"own"`+`chainFrom` (return-to-author chains; `chainDisplay:"template"`+`chainTemplate:"The {1} {2}..."` assembles blind one-word chains into a sentence)
7. `preview` — teacher-only gate before reveal (requires `content`, `approveNext`, `rejectNext`)
8. `winner` — crown with drumroll; `winnerEntry` shows WHAT they won for
9. `announce` — message to everyone; `video:` YouTube embed (host-only), `image` field; `drawingFrom` shows a drawing (announce/collect/collect-choice all have it; `_current.drawing` in foreach = Doodle Bluff rounds)
10. `collect-choice` — pick from choices; `correctAnswer`+`speedBonus` (Kahoot scoring), `choicePool`/`excludeAuthored`/`shuffle`/`foolPoints`/`poolLimit` (bluffing), `liveResults` (Live Poll: projector tally grows as answers land)
11. `ai-eliminate` — AI judges and eliminates
12. `leaderboard` — rankings; `from` accepts array of refs to sum rounds
13. `reveal-one` — one-by-one reveal; `itemTemplate` for objects, `limit` random sample
14. `team-split` — `teamCount` OR `groupSize` (never singletons); `method: random|balanced|teacher|choice`; `capacity:"open"`
15. `rank` — reorder a list; earlier-step ref OR literal item list; `teamsFrom` (team-split OR pairwise collect) = rank as groups: every student still ranks alone, each group's order is its members' average, output `byGroup` + `groupRankedList` beside `rankings`/`rankedList` (pure rule in `engine/phases/choice-draft.js`)
16. `wager` — bet points, auto or host-resolved
17. `relay` — turn-by-turn collaborative input
18. `foreach` — sub-phases per item; `limit` sample (a round per response kills the room ~round 12), `pairMode:"human-vs-ai"`, scoring `correct`/`tally`/`scores` (scores = adopt the sub-phase's own graded map; the bluff-rounds mode, keeps foolPoints), `_current`/`_foreach`/`_candidates` vars; sibling-sub-phase refs remap automatically in message/prompt/instruction/correctAnswer/input/content/choices/choicePool.from/excludeAuthored ONLY — collect's rotation/pairing fields (rotateFrom, assign, pairsFrom, ...) are schema-banned inside foreach (`contexts: ['topLevel']`)
19. `rate` — 1-N custom scales; bar + pie results; `visibility: all|host-only`
20. `turn` — charades; server timer, team rotation, `poolLimit`
21. `merge` — shared live draft (think-pair-share) with ONE PEN: writing claims it, agreeing releases it, 2.5s idle lets a partner take it; `agreeMode both|any|timer`, `groupSize` 2/3/4 OR `groupsFrom` (adopt a pairwise collect's pairs or a team-split's teams); output `merged`
22. `one-voice` — cooperative counting; server-authoritative collision window
23. `end` — game over
24. `buzz` — first-tap-wins buzzer; outputs `scores`
25. `estimate` — numeric guessing; `scoring: closest|graduated`; no answer = poll mode; `min`+`max` (or "on a scale of 1 to 10" in the prompt, read by `engine/phases/estimate-range.js`) = a row of numbers to tap (up to 12) or a slider, never a bare number box
26. `match` — pair two lists; `pairs` + `pointsPerMatch`
27. `sort` — items into buckets; all-or-none correct buckets = graded vs consensus
28. `checklist` — group to-do list with live progress; `items` (strings or `{text, role}` role-tagged) + optional `teamsFrom` (team-split OR pairwise collect, pairs share a list) + `rolesFrom` (team-roles; tags items as a role's job, viewer's own highlighted); no scores
30. `solo-quiz` — self-paced multiple-choice quiz (`questions` [{question, choices, correct}], `showAnswers`, `pointsPerQuestion`); per-student progress, projector shows a progress board only, two-stage close, output `scores`; built for rolling start
31. `assign` — Hand out choices (2026-09-16, `engine/phase-handlers/assign.js` + `engine/phases/choice-draft.js`): after a rank step (`from`), every group (the rank had `teamsFrom`) or every student gets ONE of the ranked items, first choices first, round by round in a fresh shuffled order, spots per item = `perChoice` or ceil(units / items), a unit with every pick full gets the least-taken item; output `assignments`, `byPlayer` (`{{X.mine}}` = your group's item), `byChoice`, `choiceRank`, `assignedList`, `board`; host-paced (one card per group on the projector, "Your group got:" on the student screen, i18n rows in every table); the Choice Draft recipe is the model (teams → rank as groups → assign → a wrap announce reading `{{draft.mine}}`)
29. `team-roles` — job per group member (Facilitator/Recorder/...); `teamsFrom` (team-split OR pairwise collect) + `roles` + `method: random|choice` (choice = claim-a-role with per-group capacity ceil(members/roles), stragglers auto-filled at close); output `byPlayer` so `{{X.mine}}` = your role, `rolesList` = the lineup

AI task types: `summarize`, `generate` (Haiku); `generate-choices`, `compare`, `rank`, `judge` (Sonnet).

## Key Files
- `server.js` — Express + socket handlers + template resolution
- `engine/` — GameEngine, StateMachine, PlayerRegistry, RoomManager, GameLoader (validation), phase handlers in `engine/phase-handlers/` + pure logic in `engine/phases/`
- `engine/phase-schemas.js` + `engine/resolver-grammar.js` — single sources of truth (phase fields; `{{...}}` syntax)
- `services/ai-service.js` — all AI calls via `_callClaude` (budget-gated by `services/ai-budget.js`); game review; mock mode
- `services/simulator.js` — robot playtest + chaos mode
- `screens/` — host, player, teacher, designer (editor.js/simple-view.js/builder), shared (design.css, juice.js, bot-brain.js, drawing.js, speech-input.js, feedback-widget.js, meadow.js — the post-submit waiting field, a SHARED space since 2026-08-30: server-assigned canonical block indexes + relayed nudges via `engine/meadow-sync.js`, payloads are anonymous {index, fx, fy} only)
- `games/*/config.json` — game configs; `recipes/*.json` — recipes; `recipes/prompt-banks/` — prompt banks (per-prompt attribution is load-bearing)
- `db.js` — Neon Postgres; `engine/room-snapshot.js` — restart survival
- `scripts/` — sim-harness.js + simulate-*.js playtest scripts, screenshot.js, demo-room.js

## When Adding a New Game
1. Copy games/_template to games/your-game-name
2. Edit config.json for simple games
3. Edit game.js only if you need custom logic
4. Add it to the snapshot map in `tests/engine/validator-diagnostics.test.js` (fails loudly on unknown games)
5. Test with: `npm test`, then `node scripts/simulate-any-game.js <game-id>` (server running)

## Safety (implemented)
- Content filter (`engine/content-filter.js` + blocklist) on submit-response, merge-draft, relay-submit
- **Moderation ladder** (`services/moderation-ladder.js`, needs `OPENAI_API_KEY`): OpenAI moderation scores block the obvious (≥ blockAt rejects), the uncertain band goes to Haiku (`AIService.moderateText`), Haiku's "unsure" flags the entry on the teacher console ("Needs a look" chip via `responseFlagged` → `buildSubmissionList.flagged`; teacher surfaces only, never the projector). On collect submits + relay lines; NOT merge drafts (per-keystroke) or drawings (owner's call). Fail-open on OpenAI errors, fail-to-teacher on Haiku errors; sim rooms and PII-scrubbed text only
- Drawing safety = attribution + teacher-console thumbnails + preview gate (filter can't read pictures)
- Crash isolation: try/catch on every socket handler; unhandledRejection logs, never crashes
- Host moderation (hide/kick); PIN brute-force lockout (`engine/pin-throttle.js`)
- AI: SAFETY_RULES in every game-run prompt; name-stripping + PII scrub (see Standing Rules); cost guards (per-minute throttle + Neon-persisted daily cap, 429s)
- XSS output encoding enforced by test
- Anonymous mode: top-level `anonymous: true` (editor "Student names" setting) — server assigns "Color Animal" play names (`engine/anonymous-names.js`), typed names discarded unread; player join form hides the name box via `GET /api/rooms/:code/info`
- Not yet: rate limiting on non-AI socket events, PII redaction

## Environment
- `.env`: `ANTHROPIC_API_KEY` (real AI; absent = mock mode), `DATABASE_URL` (Neon; absent = filesystem), `SITE_PASSWORD` (site OWNER: feedback inbox, owner mode, built-in edits, teacher-console credential — the site itself is public), `AI_CALLS_PER_MINUTE` (default 20), `AI_DAILY_CAP` (default 500; 0 disables), `OPENAI_API_KEY` (moderation ladder; absent = blocklist only), `MODERATION_BLOCK_AT`/`MODERATION_REVIEW_AT` (ladder thresholds, defaults 0.85/0.4), `OPENAI_MODERATION_URL` (test stub override), `POSTHOG_KEY` (site analytics relay; absent = off) + `POSTHOG_HOST` (default `https://us.i.posthog.com`)
- Local `.env` points at a Neon **dev branch** (since 2026-08-30), isolated from prod: local DB writes never reach the live site, and owner ★ flips / built-in edits must be done on jamyard.org itself (the live domain since 2026-09-10; jamyard.xyz was the first). Refresh dev data via the branch's "Reset from parent" in the Neon console.

## Testing
- `npm test` — all Vitest tests (~4s; count in Current Snapshot)
- `node scripts/simulate-any-game.js <game-id>` — universal playthrough (server running)
- `node scripts/simulate-chaos.js [gameId] [--players N]` — school-wifi chaos suite
- `node scripts/simulate-restart.js` — restart-survival proof (needs DATABASE_URL)
- `node scripts/audit-fit.js <outDir> <game...>` — fit audit: every projector (1920x1080) and student (1366x768) step measured against its viewport through Try it out, screenshots of the ones that overflow (starts its own server on :3005)
- `node scripts/a11y-audit.js [baseUrl] [--detail]` — axe-core over headless Edge on the eight main pages, violations by page (server running; exits 1 on serious/critical)
- Named sims: simulate-closer/snowball/one-voice/team-modes/teacher-console/console-skip/append-only/late-join/late-seating/holding/approve-vote/secret-handout/ready-made-trust/show-the-chains and others in `scripts/`; `simulate-submit-race.js` spawns its own server + slow moderation stub (four students, immediate Close)
- CI: `.github/workflows/test.yml` on every push/PR; deploys only on green via `RENDER_DEPLOY_HOOK`

## Design Documents (docs/)
- **NEXT-STEPS.md** — living roadmap; check FIRST when asking "what should we work on?" (its "START HERE next session" block is current)
- **DESIGN-PHILOSOPHY.md** (engineering why) · **PEDAGOGY.md** (learning why) · **ARCHITECTURE.md** (what/how)
- **COMPARATIVE-ADVANTAGE.md** — positioning thesis + licensing gates (email pzlearn@gse.harvard.edu before public launch); research notes in `docs/research/`. Read before designing new activities.
- **COMPLIANCE-TODO.md** — COPPA/FERPA/§49073.1 checklist
- **PROJECTOR-STYLE.md** · **SURFACES-PLAN.md** · **LIBRARY-FIRST-PLAN.md** · **RECIPE-LAYER.md** · **connection-pack-spec.md** · **SAFETY-DESIGN.md** · **AUTHORING-DESIGN.md** · **GAME-CONFIG-DESIGN.md** · **AI-TASK-DESIGN.md**
- **CHANGELOG.md** — dated log of everything shipped (append new entries there) · **DEFERRED-IDEAS.md** — parked
- **CLAUDE-ARCHIVE.md** — full historical detail formerly in this file (ship-log, per-game descriptions, implementation notes)
