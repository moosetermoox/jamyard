node_modules/
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# User-saved recipes — per-user content, lives outside the repo
recipes/user/

# User-saved games — per-user content, lives outside the repo
# (Built-in games stay under games/ at the top level.)
games/user/

# Local-dev feedback inbox (deployed sites store feedback in Neon)
data/
