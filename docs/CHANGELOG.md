# Changelog / Refinement Log

> Moved out of `CLAUDE.md` on 2026-05-19 to keep project instructions lightweight. This is the chronological build log; it is not loaded into every session.

- 2026-08-08 (editor header hierarchy): **Five shouting buttons become one primary** (the field test's last open design item, Sam: "the eye can't find THE action"). The cobalt header now has exactly three documented button roles (comment block in editor.css): PRIMARY = solid go-live green, one per bar (▶ Host — Sam suggested blue, but blue-on-cobalt disappears; green matches the projected START! semantic product-wide); ATTENTION = solid yellow, state-driven only (Save while dirty — pure CSS via the `save-status-dirty ~ #save-btn` sibling selector, zero JS changes); QUIET = white-outline ghost (More, Check for Errors, Preview, Save-when-clean — new `.btn-ghost`; the old purple `.btn-review` and same-yellow `.btn-secondary` pills leave the header). `white-space: nowrap` keeps the pills one-line. No behavior changes; no JS referenced the removed classes. Screenshot-verified clean + dirty states at 1440px; 998 tests.
- 2026-08-08 (host pre-room redesign): **The projected screen joins the design system** (field-test backlog #1, Sam: "the brand's most public moment and its least designed surface"). Picker: a proper card (headline "Host an activity", sub-line, uppercase label, design-system-styled select, description, "Browse the full library instead" link) replaces the bare label + native select; the wordmark h1 shrinks to a mark (2rem) so content owns the screen. Lobby: roster is now avatar chips that fill in as the class arrives (rhymes with the student lobby's holding chips; same `Juice.avatarFor`), "Who's in" heading with a live count badge, and a real empty state ("Waiting for students to join..."); the disabled START finally says why ("Start unlocks when the first student joins." at zero; below the activity's minPlayers an informational "Made for N+ students, M in so far." — the gate itself is unchanged: unlocks at 1). Plumbing: the socket `get-games` listing and both `/api/games` paths now carry `minPlayers` (falling back to the lobby phase's value — 2 configs declare it there), and the `?game=` deep-link path calls `updateDesc()` after programmatically selecting (no 'change' event fires, so the description + minPlayers stash read the WRONG game on every library deep link — latent since the deep link shipped). Markup/CSS/copy only, no flow changes; kick buttons and 2026-07-27 lobby behaviors intact. Screenshot-verified all three states (picker, empty lobby, 2-in lobby); 998 tests + full chaos suite green.
- 2026-08-08 (privacy page + a11y polish): **`/privacy` is live** (field-test backlog: "the safety engineering is the product's best-kept secret" — a teacher can't cite source code to a principal). Plain-language page in the design system: shortlist card (no accounts, activity-lifetime data, names never to AI, nothing sold/trained-on), the full retention table the compliance review requires (names every room-snapshot content: responses, drawings, player names, reconnect tokens, teacher PIN; plus activity_runs, user games/recipes, feedback with its anonymous-by-design note), AI section with the defensible pii-scrub claim + the honest limit (student-typed text is the answer; teacher moderation is the control), device-local storage, security posture. Linked from home footer + library footnote; route `app.use('/privacy')`. **Two yellow placeholders remain (operator name, privacy contact email) — fill before pushing to prod.** COMPLIANCE-TODO §1 updated. Also the field test's a11y batch: the `--focus-ring` token finally has consumers (global `:focus-visible` rule in design.css for button/a/input/textarea/select — student and host buttons rode browser-default focus; page-specific rules still win by load order), mic button 30px→40px hit area at 0.55 opacity (touchscreen Chromebook minimum; glyph unchanged, input padding 36→44px), and the detached library-search mic fixed generically (attachMic transfers the input's computed max-width onto the wrapper, so a capped input inside a full-width flex row keeps its mic in ITS corner). Screenshot-verified; 998 tests.
- 2026-08-08 (library findability): **Subject searches stop dead-ending** (field-test backlog #2: Maria's first instinct was searching "history" — zero matches, because the activities are topic-agnostic shells and nothing said so). Three parts: (1) new optional top-level `keywords` config field (added to `GAME_CARD_META_FIELDS`, so both listing paths carry it; phase-level allow-list untouched) seeded into the 12 public-facing configs with subject/topic terms ("history", "debate", "vocabulary", "think pair share", "any subject"...) — "history" now finds Both Sides of the Rope + Whose Eyes; searches match keywords in BOTH the library and the designer grid. (2) **Search rescue**: a query with zero hits no longer renders "No matches" — instead an honest note ("Nothing is named 'X', and that's okay: these activities work with any subject...") + the concierge link above the full shelf (goal filter still respected). (3) `?q=` deep link lands on the library with a search applied. Bonus: group-work-day finally has tags (group work/stations/projects — it was invisible to every chip). NOTE: the thin Connect chip is partly live featured-override curation in Neon (one-voice flipped off) — a DB decision deliberately not touched here. Screenshot-verified both states; 998 tests.
- 2026-08-08 (holding screens): **Waits show the room, not a void** — the field test's #1 convergent finding (all three testers; Sam's "one big design move" = Jayden's "one change") shipped as a holding treatment across every player wait. Server: two new room-wide events — `room-roster` (lobby ONLY: names + count; the projected host roster already makes this public, but mid-game the roster stays off student devices) broadcast from join/reconnect/disconnect/grace-expiry, and `room-progress` (**counts only, never names** — submission status on a student device must not create who's-slow pressure) broadcast from submit-response and submit-vote. Player: lobby shows "N of us here" + avatar chips (Juice.avatarFor, pop-in animation) as classmates join; submitted + vote-submitted screens show a live "X of Y in" line + what-happens-next copy; process/game-waiting screens get pulsing holding dots (reduced-motion safe); stale counts reset at each game-started/vote-start. Copy: "AI is working on something special..." → "Reading everyone's answers..." (Jayden: "homework in a party hat"). Verified by new `scripts/simulate-holding.js` (7 checks: roster completeness, counts-only payload keys, every-player delivery, lobby-only roster) + 420px screenshot of the live lobby; 998 tests + late-join sim still green.
- 2026-08-08 (persona field test + fix wave): **Three agent testers** (history teacher / bored student / creative front-end dev) ran the live build independently; report published as an artifact, full texts in session transcript. Four fixes shipped from the ranked list: (1) **Late-join stranding** — the join-room NEW-player branch never called `sendCurrentState` (only reconnects got it), so a student joining mid-game stared at lobby copy AND silently blocked every all-submitted auto-advance (the eligible count included them). Two testers hit it independently in live rooms. New joins now drop into the current phase (onReconnect handlers already tolerate never-seen players: collect offers the input, merge/vote/team-split show contextual waits), wrapped so a failure degrades to the waiting screen, never a failed join. Verified by new `scripts/simulate-late-join.js` (7 checks incl. late submit counted 1/4) + full chaos suite green. (2) **Escaped em dashes** — 6 student-facing `'—'` literals (player + host leaderboards/winner attribution) dodged the no-em-dash scanner two ways: the file-level `includes('—')` short-circuit never saw a file whose only dashes were escapes, and the raw-slice token check can't decode them. Strings fixed (`Name: 50 pts`, `by Maya`); scanner now checks acorn's DECODED token values + escape-aware short-circuit, with escaped-form self-tests. (3) **Customize revise envelope** — Sonnet sometimes returns the revised config BARE (valid JSON, no `{updatedConfig, summary}` wrapper) → "AI response missing updatedConfig" → every library Customize silently degraded to a plain copy. Now a top-level object with `phases` is accepted as the config; prompt states the envelope rule explicitly; max_tokens 4096→8192 (full-config responses truncated). Verified with a real API call on the field test's exact failing scenario (whose-eyes + "Reconstruction / 11th grade": 200, 6 phases, real summary). (4) **Concierge params clamp to recipe min/max** — `suggest-validate.js` accepted any number for a known param name, so the AI's minutes (`evidenceTime: 4`) landed in seconds fields (min 60) and create failed with an error the teacher didn't cause. Numeric params now clamp into `[min, max]` (non-numeric values for numeric specs dropped), and the suggest prompt lists numeric params with label + range ("Evidence time (seconds), 60-300") so the model fills legal values in the first place. **998 tests** (7 new). Remaining field-test backlog (wait-state holding component, library subject-search dead-end, privacy page, editor header hierarchy, host pre-room styling, mic touch-target) not started.

- 2026-08-08 (late night): **Builder compatibility affordances** (from the teacher's "are any steps incompatible?" audit). (1) The silent AI palette tile is fixed: clicking "AI transforms answers" with no question step earlier now explains itself ("Add a question step first") instead of doing nothing, in both the palette path and the flavor-pair path (buildAiPair null was swallowed). (2) Dependency-aware rail nudges: skeleton steps with upstream needs (leaderboard/winner/eliminate need a scored step, foreach/reveal-one need a question step, turn needs a team split plus phrases) say so the moment they land via a STEP_NEEDS map, instead of waiting for save-time validation; a reveal-one inserted with no source is now also treated as needing setup. (3) **Puzzle-piece step cards**: every Builder card grew a tab bumping out of its bottom edge and a notch cut into the next card's top edge (Scratch-style, pure CSS pseudo-elements, background:inherit so colored cards keep their color, selected cards get blue connectors, lobby has no notch and end has no tab), so the stack reads as pieces that snap together. Screenshot-verified on fix-one-thing + a blank-activity behavior probe (alert captured, leaderboard nudge rendered). 991 tests.
- 2026-08-08 (night): **"Not sure what to make" concierge** (teacher's idea, shaped in a brainstorm: situation-first teachers get a guide; suggestions LEAD with running existing things per library-first; fixed questions, one AI round). Entry: link under the Create idea box + the library's empty-search dead end (/designer?notsure=1). Three fixed questions (occasion chips from the goal vocabulary, optional topic with mic, time chips), then one Sonnet call whose output may ONLY reference real things: {kind:host|recipe|storyboard}. Structural guarantee in pure `engine/suggest-validate.js` (8 tests): host ids must exist in the featured set, recipe ids must exist with unknown params stripped, storyboards must be 2-12 known bricks; anything else is dropped server-side before render. Cards show REAL catalog data (server-enriched name/description/playTime) + one AI why-line; actions are native: Host this, Use this recipe (form opens prefilled via data-param-name, so recipe validation applies unchanged), Plan it step by step (seeds the existing storyboard approval flow, which still compiles through validated builders). Honesty inherited: the prompt names what the platform cannot do, and a "note" surfaces limits. Live-verified: water-cycle review returned 3 legal recipe fills (0 dropped); a record-videos ask returned "Lanyard can't record or play back videos" + nearest-possible activities. Empty suggestions and every failure path route to the library. 991 tests (11 new).
- 2026-08-08 (evening): **Em dash sweep + standing rule** (teacher: students read em dashes as an AI tell). ~500 user-facing instances replaced across 102 files via `scripts/sweep-em-dashes.mjs` (heuristics: numbered lead-ins get colons, line-end codas get colons, sentence breaks before capitals, commas before continuations; JSON string values walked, JS STRING LITERALS only via acorn tokens so comments stay untouched) + hand polish. Scope: game configs, recipes, closer/lanyard banks, screens HTML, screens/engine/services JS strings. Exempt: along.json (attributed third-party prompts kept verbatim), code comments, docs; en dashes in ranges (20–45 min) kept. AI output covered at the single choke point: `STYLE_RULES` ("Never use an em dash...") appended to every outbound system prompt in `_callClaude`. Enforced forever by `tests/style/no-em-dash.test.js` (configs/recipes/HTML byte scan + acorn string-literal scanner with self-test; the style rule's own mention allowlisted). New devDep: acorn. 980 tests; both-sides-rope robot playtest clean on the reworded copy.
- 2026-08-08 (later still): **Customize asks first** (teacher's idea: a few AI questions before the editor, pre-filling the copy for their purpose). Library Customize now: (1) new `POST /api/games/customize-questions` → `AIService.generateCustomizeQuestions` (Haiku, config-only, no student data; mock mode returns canned questions; failures return an empty list, never an error) asks 2-3 activity-specific questions — live test on Vocab Match produced "What language and topic do you want to teach?" etc.; (2) a "Make it yours" dialog (answers optional, corner mics on every box via autoAttach, "Skip — just copy it" always available); (3) answers route through the EXISTING validated `/api/games/revise` flow (words only, structure/timers/tokens preserved, structural-error check client-side) — live test turned French Vocab Match into "Spanish Food Match" with real manzana/pan/leche pairs, 0 structural errors, identical phase ids; (4) every failure path (no questions, budget 429, revise error, structural errors) falls back to the plain copy — Customize can never do less than it did before. Save logic factored into `saveCopyAndEdit`. 976 tests (3 new).
- 2026-08-08 (later): **Landing page refresh** (teacher's words): headline "What are we going to do together?", tagline "Simple activities for connecting, thinking, and fun — together.", and a **library carousel** beside the Find an Activity card — rotates through the same activities the library shows (shared game-visibility filter + MyGames, so featured + this-device only), one card at a time (name, clamped description, playTime, 🕊 no-winners line), auto-advance 4.5s (off under prefers-reduced-motion), pauses on hover, ‹ › arrows, whole card links to /host?game= ("Host this"). Decoration-grade failure mode: fetch error = no carousel, page unaffected. Includes the recurring [hidden]-vs-display:flex override.
- 2026-08-08: **Two step-picker gaps closed** (teacher's catches). (1) **Builder's "Browse all steps →" did nothing visible**: it expanded the LEFT palette — hidden behind the popover the teacher was looking at — and palette tiles insert at the END anyway, not into the opened gap. Now the popover itself expands into the full grouped step list ("Pick any step", same palette groups/colors) and picking one inserts into THAT gap; "← Back to suggestions" returns. New `browseAllGap` state, cleared on insert/close/other-gap-open. (2) **Simple's "Add a Step" showed 18 of 28 types**: `PHASE_CATEGORIES` predated turn/merge/one-voice/buzz/estimate/match/sort/checklist. Rebuilt as ONE taxonomy with the Builder palette (same verb groups: Ask/Show/Decide/Team up/Rounds/AI) covering every type incl. the Secret + Clue brick — `addPhaseOfType` now prefers `StepSuggestions.defaultPhaseFor` (certified hostable defaults; turns collect-two into its real two-field collect) before the per-type default chain. PHASE_CATALOG gained the missing merge/one-voice/collect-two entries. Screenshot-verified: gap popover full list, Simple picker all groups, Secret+Clue insert producing `{type:collect, fields:2}`. 973 tests.
- 2026-08-07 (night, later still): **Templates consolidated into recipes** (teacher's observation: "recipes and templates are almost the same thing" — they were: a template is a recipe with zero parameters, minus the guided form, the AI matcher, prompt decks, and DB persistence). The template picker and 5 content templates are gone; every one had a recipe twin (Simple Poll→class-poll, Quiz Show→quiz-show, Elimination Game→elimination-tournament, Creative Writing→story-builder/creative-vote) EXCEPT Question & Share, whose deliberately zero-AI shape (Marcus's finding — raw answers on the projector, no summarize step) is now **`recipes/question-share.json` (19th recipe)**: params question + timer, compiles to the template's exact lobby→intro→ask→share→end chain (live-compiled, zero AI verified). Create page side doors simplify to "Browse recipes · Start from scratch" (blank-into-editor). `templates.js` survives blank-only for the editor's createBlankConfig and is no longer loaded by the Create page. "Show me a complete example" is covered better by library Customize (clone any built-in). 973 tests green (recipe-schema suite validates the new recipe automatically).
- 2026-08-07 (night, later): **Library doorway goes STRAIGHT to Create** (teacher's second pass on this flow: the doorway dialog asked "what do you want to make?" and then the Create page asked again — redundant; supersedes the same-evening ?idea= carry, which papered over a dialog that shouldn't exist). "Build your own activity" now navigates directly to /designer; the question is asked exactly once, in the idea box with the mic. The builder-request signal still files quietly on the way (fixed message, no note field), and the lanyard-builder localStorage flag still sets. Screenshot-verified: one click → Create an Activity.
- 2026-08-07 (night): **Legacy whole-config generator RETIRED** (teacher's call: "if you can't put it together using the steps, you shouldn't be able to make it — too many possibilities for things breaking"; also the long-standing DECIDED in the storyboard notes). Removed: the no-match view's "Generate Custom (Advanced)" button, the whole legacy client flow (showLegacyAIGenerateModal/fetchClarifyQuestions/createFromAI, 280 lines), routes `POST /api/games/generate` + `/api/games/generate-questions` (now 404), and AIService.generateQuestions/generateGame/_generateGameMock/_generateGameReal/_autoPolishConfig + CLARIFY_QUESTIONS_PROMPT (224 lines). KEPT deliberately: `GAME_GENERATOR_PROMPT` and `_fixGeneratedConfig` — the Ask-AI revise flows (whole-game + per-step) build on them; `/api/games/generate-theme` untouched. Custom creation is now recipe match or storyboard-only — AI writes words, deterministic compileStoryboard writes structure. No-match buttons: Cancel / Pick from Recipes / Plan it step by step. 973 tests green; live-verified (endpoint 404, clean no-match dialog).
- 2026-08-07 (evening): **Library doorway carries your idea into Create** (teacher's catch: dictate your idea into the Build-your-own note, land on Create an Activity, and have to say it all again). "Start building" now rides a real note (10+ chars, the Create page's own launch threshold) through the existing `?idea=` deep link — the idea lands IN the box and the from-description flow auto-launches; shorter/empty notes go to plain /designer as before. The builder-request feedback signal still files either way; placeholder copy now says both destinations honestly. Flow-verified: dialog → typed idea → landed on Create with the matcher already running.
- 2026-08-07 (even later): **Mic became a corner icon + library coverage** (teacher feedback: "transparent icon in the corner of text boxes", and the library's Build-your-own dialog had no mic). attachMic redesigned: the input moves into a `position:relative` `.mic-wrap` (same node — every existing reference/handler keeps working; flex-row parents detected and the wrapper takes over the flexing) and the mic renders as a quiet transparent 🎤 absolutely positioned top-right INSIDE the box (opacity .4, hover/live full; input gets paddingRight so text never runs under it; blocked mic = dimmed `.mic-off`, no text swap). `enableFocusMics` (focus-delegation) replaced by **`Speech.autoAttach()`**: eager initial sweep + MutationObserver, so every text box — present or rendered later — visibly carries the icon (student answer box, multi-field inputs, idea box, Simple/Builder/Ask AI, recipe forms, dialogs, search). Library page now loads the module + autoAttach — the Build-your-own doorway note box (and search) speak too. Player's `#response-input[hidden] + .mic-btn` hide rule survives (order preserved inside the wrapper). Screenshot-verified: library doorway dialog, Create idea row (flex intact), editor rail, live player collect. Debug note for the record: a screenshot-profile port collision resurrected an old profile's `lanyardEditorBuilder=1` and opened the editor in Builder view — looked like a regression, was stale localStorage in the CDP profile. 973 tests.
- 2026-08-07 (later still): **Mic input reaches the teacher side — talk your activity into existence.** The Create page's idea box gets a full-size "🎤 Speak instead" button in the input row (speak the idea, hit Make it), and both /designer and the editor run new `Speech.enableFocusMics()`: a focusin-delegation mode where ANY text field the teacher focuses grows a compact mic — Simple view step boxes, the Builder rail's primary-text field, Ask AI, recipe param forms, storyboard cards — with zero per-form wiring, so future forms get dictation for free (opt an area out with a `.no-mic` ancestor class; re-renders self-heal since buttons die with their fields and re-attach on next focus). Mic button styles moved to shared/design.css (one definition: `--theme-*` vars win on themed player screens, design tokens fall back on teacher chrome). Verified: 973 tests, Create page screenshot (idea row reads [box] [🎤 Speak instead] [MAKE IT]), editor screenshot with the compact mic under a focused field (headless gotcha for the record: programmatic `.focus()` fires no focusin without real window focus — synthetic FocusEvent dispatch proves the listener; real clicks work).
- 2026-08-07 (later): **Mic input — speak instead of typing.** New shared module `screens/shared/speech-input.js` (browser global `Speech` + side-effect-importable, the bot-brain pattern): `attachMic(inputEl)` renders a "🎤 Speak instead" button using the browser's built-in Web Speech API — present on Chrome/Chromebooks, feature-detected so unsupported browsers never see the button, and a blocked mic (school-managed devices) flips it to a disabled "Mic unavailable" instead of erroring. Dictation REBUILDS on top of whatever was typed when the mic opened (pure `mergeTranscript`, unit-tested — typing is never overwritten), shows interim results live, respects the input's maxLength, and fires real input events so the char counter and notices react. One dictation at a time; leaving a screen (`showSection`) or submitting stops it. Wired to the player collect textarea (button hides with it in choice/drawing/fields modes via a sibling CSS rule) and to every multi-field input (compact per-field mics). Prototype player iframes gained `allow="microphone"` so teacher previews can use it. Privacy shape: NO audio ever reaches the game server — recognition runs through the browser's own speech service (Google on Chromebooks) and only the final TEXT enters the normal submit path, where the content filter treats it exactly like typed text. Not yet wired: relay input + merge draft (same helper would drop in). 972 tests (9 new); screenshot-verified on the collect screen.
- 2026-08-07: **The crown shows what they won for** — the winner phase now surfaces the winning submission itself, not just a name and a number. Engine: pure `traceEntryRef` (explicit `entryFrom` override, else auto-traced winner `from` → vote phase → its `candidates` responses ref; literal candidate lists refused — no playerIds) + `findWinnerEntries` (each winner's own text; drawings/blanks skipped) in winner-handler.js; `_runWinner` attaches `winnerEntry`/`winnerEntries` to the phase output (so `{{crown.winnerEntry}}` works in templates — schema output fields feed the grammar) and the emit. Corn Story + Elimination Game get it with zero config changes. Fun: host screen plays a build-up beat — "And the winner is…" pulses over a new `drumroll` juice cue (one accelerating-tom score, so the per-cue throttle can't chop it) — then the crown lands: 👑 + avatar + name slam, fanfare + confetti, the winning entry on an accent-bordered quote card (ties show every tied entry with authors), staggered standings. Player screens mirror the beat quietly (sound/confetti still only on the winner's own device, "That's you!" detail line). Auto-advance pause default 5s → 10s — the crown is a payoff beat. Editor: "What they won for" dropdown (auto by default) + entry element in both live previews; `entry` toggle in hostShow/playerShow. Verified: 11 new unit tests (963), live socket probe (new `scripts/simulate-winner-entry.js` + `games/_winner-entry` fixture — entry traced, delivered to host and player, pause 10), elimination-game robot playtest (pre-existing eliminate-family stall quirk only), screenshot of the reveal state.
- 2026-08-07: **Fix One Thing** (games/fix-one-thing/) — a ~5-minute demo of the core loop: one sentence each ("what would you fix about school tomorrow?", passAllowed, 90s) → AI weaves a one-line-per-submission manifesto ("WHAT THIS ROOM WOULD FIX") → teacher preview gate → reveal. Unfeatured. Registered in the validator snapshot; robot-playtested clean (4 bots, 0 findings).
- 2026-08-03 (later still): **Decorative-scoring architecture nets + structural needs-work batch.** The two-truths defect was a CLASS, not a config: the validator checked refs/types but never "can this scoring ever award a point", and the robot playtest asserted completion but not that scores were nonzero. Three nets now: (1) validator `SCORING_NEVER_AWARDS` warning — all-zero tally pointMaps AND leaderboard/winner reading `.scores` from a phase with no scoring configured (fixtures in validator-diagnostics); (2) robot playtest emits an ERROR when a leaderboard shows with every score 0 despite bots answering everything; (3) AI-generation guidance teaches the idiom (capture the secret in its own field, never zero pointMaps, every guess loop needs a host-paced reveal beat). Structural fixes: **elimination-game** rebuilt Corn-Story-style (3 unrolled escalating rounds, eliminated players become the voting JURY, framing announces); **scamper** cut 7 lenses → 4, per-lens intro announces removed, default problem on-screen, and the payoff is now `reveal scope:"own"` — every student sees their idea's full journey; **mad-lib-mashup** lost the fake AI chaos-score leaderboard, gained teacher previews before both AI reveals + an AI shortlist so the final vote is 6 options not 25; **dream-vacation** ("Pitch" now) ranks an AI-merged shortlist of 6 and caps the relay at 8 turns (`turns` already existed); **charades-bowl** got `turn.poolLimit` (new engine field — round 1 draws ≤30 phrases, stores the draw as `.pool`, rounds 2/3 ref `round1.pool` to keep Fishbowl's same-bowl mechanic); **convince-me** reframed as write-then-optionally-read-aloud (host-paced spotlight), blameless rating labels; **punchline-showdown** generates 15 prompts (max pairs) + odd-player judge framing; **yes-or-no-bets** got a question worth betting on; **emoji-movies/class-quiz/who-said-it** surface the round's guesses/vote distribution in their reveals via existing sub-phase ref remapping (`{{guess.responses.list}}` / `{{guess.barChart}}` — no engine change needed). All sims pass; 952 tests. Known pre-existing sim quirk: eliminate-family games show a stall+force-skip in the robot playtest (corn-story control run reproduces it).
- 2026-08-03 (later): **Needs-work fixes: the scoring-promise cluster.** Two new bluffing primitives on collect-choice: **`foolPoints`** (`engine/phases/bluff-scoring.js`, pure + tested — the AUTHOR of a fake earns N points per classmate who picks it; merged into `.scores` alongside correctAnswer grading; needs excludeAuthored for authorship) and **`poolLimit`** (per-player ballot cap — samples the `from` entries, ALWAYS keeps injected literals like the truth/house lie; a 25-fake ballot on a 30s timer was unreadable). Games: **two-truths-a-lie REBUILT** — the root defect was that the game never learned which statement was the lie (one text box, all-zero pointMap, winner crowned off a zero board); now a multi-field collect (truth1/truth2/lie) captures the lie by construction, `_current.shuffledFields` + `mode:"correct"` scoring pays lie-spotters 100, a host-paced reveal-lie beat gives the author the mic, and the AI class-portrait is told which parts are invented. **definition-bluff + trivia-bluff** finally deliver their long-promised scoring (100 truth-pick + 50/fool, flat not speed), gained leaderboards, confession lines in the reveals, poolLimit 8, and honest end copy. **human-vs-ai** got its missing reveal-ai beat ("{{aiPosition}} was the AI — what gave it away?") + a discussion-hook ending. All four robot-playtested (fool-points and real per-iteration foreach scores verified in the logs). 950 tests.
- 2026-08-03: **Durable featured curation** (`featured_overrides` Neon table) — the owner ★ toggle used to PUT the whole config back onto Render's ephemeral disk, so built-in featured flips silently reverted on every deploy (teacher hit this after the coherence-wave pushes). Now: repo `featured` flags are the DEFAULTS; a new owner-gated `POST /api/games/:id/featured` writes built-in flips to Neon (user games keep the flag in their durable config; local dev without the branch-relevant conditions falls back to writing the config file — disk is durable there). Both game listings (`/api/games` + socket `get-games`) merge overrides via pure `engine/featured-merge.js` (`applyFeaturedOverrides`, tested) and carry `featuredDefault`, so the owner card shows a • drift marker with a "saved override in effect" title — drift is visible, never silent. Flipping a game back to its repo default DELETES the override (storage: "default") so the repo regains control. Override read failures fall back to repo defaults (pickers can't be taken down by a DB blip). Also: prototype page back link returns to the editor when opened from it (`from=editor`, closes the preview tab if the editor tab still exists). 942 tests.
- 2026-08-02 (later): **Day-one kit polish** — the coherence review's feature-worthy games fixed against their remaining notes. Engine: **`collect.appendOnly`** (`engine/phases/append-only.js` + server-side combine — the inherited list renders read-only and the server rebuilds responses from its own copy of the assigned text, so a vandal structurally cannot gut a classmate's list; empty additions legal so timer auto-submit passes the list through) and **rotation collects stamp `assigned` onto response records** at close (`{{_current.assigned}}` now works in reveal-one itemTemplates). Games: both-sides-rope (what-ifs reveal inserted before the re-vote — they can finally move minds; delta decluttered), whose-eyes (retype-the-viewpoint field REMOVED — also fixed the circle's silently-blank tokens: multi-field values live under `fields.*`, the old template read top-level; honest playTime), one-more-thing (appendOnly on both add rounds, thin-list rescue line, quiet-minute hostTemplate on the returned reveal), one-voice (ending honest when target unreached, big-class expectation line), someones-got-you (honest pass copy + stuck-writer fallback line), snowball (day-one framing, decide→connect tag), weekend-poem (poem scales ~1 line per 2 students with near-verbatim weaving, preview gate before reveal, "whose weekend can you spot?" beat), art-gallery (guess-aloud gallery beat, imported-joke intro fixed, hide-don't-reject preview nudge, student-facing ending), closer (all 9 share screens now say "turn to your partner — tell them what stood out", partner-name expectations, t1q1 60s, playTime ~30-40 min). New invariant sim `scripts/simulate-append-only.js` (vandal-cannot-gut + circle-shows-viewpoint). 935 tests; all 9 kit games robot-playtested clean.
- 2026-08-02: **Coherence-review action wave** (from docs/GAME-COHERENCE-REVIEW-2026-08-02.md — 41 games reviewed by 14 parallel agents for classroom worthwhileness; verdicts 7 feature-worthy / 11 fine / 19 needs-work / 4 cut; thesis games swept the top, generated leftovers the bottom). Shipped the four highest-leverage fixes: (1) **`limit` field on foreach + reveal-one** (`engine/phases/sampling.js`, uniform random sample preserving order, composes with shuffle; schema'd so validator/editor/AI-prompts all know it; AI generation guidance nudges limit on per-player sources) — applied to the 8 worst foreach-drag games (class-quiz-showdown/emoji-movies/excuse-machine 12, who-said-it/yes-or-no-bets/two-truths 10, convince-me/human-vs-ai 8 + aiInject 3→4). NOT applied where fairness is the point (galleries, encouragement walls, return-to-author). (2) **Four cut games deleted**: simple-poll (placeholder Duck/Cat/'Mouse '), llm-or-duck (one binary joke poll), roast-me-if-you-can (unreviewed AI roasts of named students projected — violates the teacher-gate rule structurally), feedback-coach-academy (weaker sibling of feedback-academy, ~75 public score-cards). Placeholder content fixed: class-critique ('happy birthday' hostTemplate + stray photo assets removed, real presentation beat, blank phase-4 → discussion prompt), lightning-round (teacher question prep cue + starter pack in recommendedFor), vocab-match (both rounds French now, 12 pairs), group-work-day (self-contained zero-prep demo tasks + reflection beat), feedback-academy (**restructured**: '[feedback text here]' placeholder + everyone-reviews-everyone 40-min foreach + whole-array coaching blobs → rotateFrom single peer review + reveal scope:'own' returns feedback to its author + class-level AI coaching, no public per-student grading; now validator-clean). (3) **Payoff phases un-timed** (16 timers removed across 13 configs): final reveals, leaderboards, and per-round author/answer reveals are host-paced — the teacher rides the laugh instead of a 5s timer steamrolling it. Rule: payoff beats are host-paced, never timed. (4) **speed-quiz earns its featured slot**: 5 unrolled questions (loopBack can't vary content), summed leaderboard, mojibake glyph fixed. All 19 touched games verified end-to-end via robot playtest (services/simulator.js, mock mode); 930 tests. Verified along the way: host/console continue clicks are stale-guarded (screens wrap socket.emit to echo phaseInstanceId — the universal sim's blind advance clicks are a sim artifact, not a server hole).
- 2026-08-02: **Projector + Builder polish wave** (teacher walkthrough findings): stray dot on every announce = the recurring [hidden]-vs-display bug (empty phase image) — overrides on host AND player; `responses.list` renders as numbered cards; player screens got the full headline/body/list formatting (renderPlayerMessage); Preview's players slider stays live (auto-launch had disabled it — disabled inputs fire no events — moving it now resets+relaunches); Builder palette unified into one expandable taxonomy (all 28 types, same groups/labels, in place — old picker modal retired from Builder), uniform tiles + "fill in its settings" rail nudge replacing the unreadable dashed border, "Loop this section" hidden from the rail (one game uses it; recipe covers it), + circles quieted. /doctor run: setup healthy, auto permission mode now the user default; CLAUDE.md trim (~62.7k chars, over threshold) offered and deferred.
- 2026-08-02: **Question & Share template** (usability backlog #2 — Marcus: the most common teacher want required amputating AI from Creative Writing). New template, second in the picker: intro announce → open question (90s) → all answers on the projector → wrap. Validator-clean.
- 2026-08-01: **Storyboard-before-generate + the bricks that make it honest** (from "why couldn't Make It build Emoji Movies?"). (1) **Two new Builder bricks**: `collect-two` (Secret + clue — two-field collect, first hidden until reveal) and `guessing-rounds` (buildGuessingRounds — THE party-game shape: foreach over the last collect, show clue → everyone types a guess → reveal secret + author; pair-aware refs when the source is secret+clue). Surfaced in the palette (Ask/Rounds groups) and suggested after any collect. (2) **compileStoryboard** (pure, in step-suggestions.js): AI proposes a SEQUENCE OF BRICKS with words — never config JSON; the compiler assembles it through the same validated builders the + buttons use, auto-appends the wrap-up, and reports problems as plain sentences. (3) **POST /api/games/storyboard** — Sonnet gets the brick vocabulary and the teacher's description, returns the storyboard (budget-guarded; no student data; no-decorative-emoji rule in prompt). (4) **Approval UI on the Create page**: the no-match view's primary option is now "Plan it step by step" — the storyboard renders as editable step cards ("Nothing exists until you do"), Build compiles client-side, saves, and opens the Builder. **Proof: the teacher's exact Emoji Movies description → AI chose announce → collect-two ("Movie Title"/"Emoji Description") → guessing-rounds → end → compiled with 0 problems → validated 0 errors → robot playtest completed, 0 findings.** AI writes words; deterministic code owns structure. 922 tests (10 new).
- 2026-08-01: **Emoji Movies** (game #32, hand-built from the teacher's description after an AI-generate attempt): everyone secretly types a favorite movie + describes it in emojis only (multi-field collect), then a shuffled foreach cycles every clue — emojis on the projector, class types guesses (30s), host-paced "It was… X! (described by Y)" reveal. Uses {{_current.fields.*}} multi-field template refs + foreach playerName normalization. Verified by the REAL robot playtest (services/simulator.js — all 4 clue rounds played, 0 findings); the universal sim script mishandles timer-less announces (buffered-event misalignment, logged as a known sim limitation — the game is fine, the crude sim isn't). Unfeatured: visible via owner mode; ★ it to make it public.
- 2026-08-01: **Agent usability tests + first fix wave.** Three fresh-eyes agent testers ran the live site: "Dana" (teacher running an activity — source-blind, screenshot-only), "Marcus" (teacher building from scratch, same rules), and a technical sweep. Dana: home/library excellent (4 clicks to a hosted room), sentence editor "the best surprise"; flagged the duplicated-topic trap, the code-only lobby, Preview/Prototype naming whiplash, no host-from-editor. Marcus: built a runnable activity in one sitting, palette taxonomy and pretend-players mode praised; flagged silent edit loss (twice), no open-question template, Simple-view dead-ends, jargon validation errors, Simple/Builder step-name mismatches. **Fixed same day:** (1) projected lobby now says "Go to ⟨host⟩/player and enter this code" under the room code; (2) editor header gained **▶ Host** (validating save → /host with the activity loaded); (3) /prototype is now titled **Preview** everywhere (editor button + home footer renamed), auto-launches when an activity is pre-selected, and shows a pick-and-launch hint instead of a blank stage; (4) **beforeunload warning** when the editor has unsaved changes; (5) Simple view's stale "insert data in Advanced" placeholder reworded. Remaining findings logged in SURFACES-PLAN §usability backlog. Agent test artifacts cleaned up (8 test games deleted).
- 2026-08-01: **"All settings →" leads to the Builder, not the stranded canvas** (teacher's catch: after the Advanced demotion, Simple view's per-step "Advanced settings →" link dumped you on a surface the pill no longer names — no context, no way back but the menu). Simple's link (relabeled "All settings →") and the review-panel/validation deep-link path (`selectPhase` from Simple) now call `window.__enterBuilder(stepId)` — the Builder opens with that step selected, its card scrolled into view, and the full settings form in the rail; the technical canvas remains the fallback only if the Builder failed to load. Subtle race fixed en route: enterBuilder's Activity-tab reset runs an ASYNC deselect that nulled a selection landing right after it, so a targeted entry now skips the reset entirely (the target parameter exists precisely because of that race). Screenshot-verified: Simple → All settings lands selected in Builder.
- 2026-08-01: **Advanced demoted to "Technical view" + Builder gains reorder/delete** (teacher's call: with Simple and Builder, Advanced stops being a peer). The editor pill is now **Simple | Builder**; Advanced lives under More ▾ as "Technical view" (same element id, so every existing wiring — simple-view's toggle, Builder's programmatic entry, review-panel deep links — still works; kept as the escape hatch where every possible config is guaranteed visible, revisit after field tests whether it can go entirely). Builder step cards grew a quiet hover tool cluster: **↑ ↓** call new pure `StepSuggestions.moveStep` (pointer surgery on the next-chain only — branch fields like approveNext/nextByWinner untouched; refuses lobby/end/illegal moves; 4 new tests incl. validator pass after moves) and **✕** confirms then reuses the editor's own `deletePhase` (full re-link logic). Found+fixed: leaving Builder re-appended the settings sidebar on the WRONG side (appendChild vs insertBefore-firstChild). 917 tests; screenshot-verified pill, menu, live reorder, and Technical view round-trip.
- 2026-08-01: **The Builder — the mockup became a real editor mode** (teacher: "let's build it"; SURFACES-PLAN Phase 3, designed across six mockup rounds same day). New **Builder** pill between Simple and Advanced: left = step palette (curated v1 set, click-to-add, grouped Ask/Show/Decide/AI); middle = the activity as plain-English cards with a **+** in every gap — ranked next-step suggestions from `screens/shared/step-suggestions.js` (pure module: transition frequencies mined from the built-in configs, arc detection, hostable-as-is defaults; every suggested step is validator-certified by test); right = rail with Activity/Step tabs hosting the RELOCATED real panels (#settings-panel + the portable #phase-config-form/live-preview — zero duplicated forms, every existing edit path intact) plus a Builder-owned primary-text field (prompt/message/template/instruction, live card sync). The whole mockup journey works: empty canvas opens "What happens first?" (announce most common — 33 of 40 built-ins), momentum (clicked suggestions land selected with the next + open), AI tiles swap to the flavor question and land as an ai-process+reveal PAIR with pre-written instructions, "Wrap it up" joins once the arc exists, and the finish panel (Try with pretend players / Host live / Keep polishing) appears when arc+end are both true (a blank lobby+end activity correctly does NOT count as finished). Integration trick: entering Builder clicks the Advanced pill first so simple-view's selectPhase wrapper never flips views; found+fixed: the editor's click-outside-collapse deselected every Builder click (re-render detaches the clicked tile, so closest() failed — guard is the body class). Verified: 913 tests (21 new incl. hostable-as-is enforcement; xss scanner extended to both new files), screenshot review of all states, and a robot playthrough of an activity assembled purely from suggestions (0 errors). Simple view untouched and still the default.
- 2026-08-01: **Surfaces Phase 1 — the duplicate shelf is gone** (docs/SURFACES-PLAN.md; teacher-approved: one-shelf merge, before field tests). `/designer` is now the **Create page**: idea box + Browse recipes + Start from scratch, retitled "Create an Activity", NO activity grid (the grid code in designer.js is element-gated and awaits a pruning pass). The **library is the one shelf**: it gained everything the designer grid uniquely had — Delete for this device's activities (shipped same day), owner mode (unlock link at the grid bottom, owner bar with feedback-inbox link, ★ featured toggles on cards, built-in edit/delete when unlocked). Flows re-pointed: editor "Back to List" → "Back to Library" (always /library; the from=library param hack removed at the HTML level), prototype back link → /library, home footer "Activity Designer" → "Create an Activity", library doorway button → "Start building" (still files the builder-request signal). Mental model: **find it in the Library, start it in Create, shape it in the Editor.** Verified: 890 tests, screenshots of the Create page (recipe picker still opens), library owner link, delete on own cards.
- 2026-08-01: **Card declutter — player-count and step-count badges removed** (library + designer grids). The player range said "roughly a whole class" on every card, and step counts are engine internals a teacher can't act on — play time already answers the real question ("does this fit the period?"). Cards now show time + goal tags + actions. `formatClassSize` helper deleted with its only caller; configs keep their `classSize`/`minPlayers` metadata (still used by validation and the host screen).
- 2026-08-01: **Hover tooltips on every ambiguous button** (user request: "Bot Fill" meant nothing until you clicked it). Native `title` attributes — reliable in tight toolbars, keyboard-discoverable, zero gameplay risk — added across: prototype controls (Launch / Bot Fill / Reset / carousel arrows), editor header (Simple/Advanced toggle, Ask AI, Save as Recipe, Check for Errors, Prototype Mode, Save, Generate Colors, Show Preview), host lobby (Open the room, Copy join link, QR) + preview Approve/Reject + one-voice speaker toggle, teacher console (Start activity, Approve/Try again, Close submissions, Next step, per-entry Hide/Kick with their consequences spelled out), library/designer card Host + Edit buttons, and the designer's "Make it". Tips say what happens next in plain words ("Auto-fill an answer for every practice player so you can move through the steps quickly"), matching the continue-labels philosophy: buttons say what they do.
- 2026-08-01: **Site-wide design pass — one theme, real typefaces, no emoji clutter.** (1) **Self-hosted fonts**: new `screens/shared/design.css` (linked by every page) ships Archivo Black (display — a better-drawn drop-in for Arial Black, same pop-art weight) and Nunito 400/700/800 (body — rounded, readable on Chromebooks) as latin woff2 (~68KB total, `screens/shared/fonts/`, no external requests so school filters can't break them) plus the shared design tokens (ink/canvas/brand colors, radii, pop shadows). Every `font-family` stack across all nine screens swept to `'Archivo Black', 'Arial Black', ...` / `'Nunito', Arial, ...` — graceful fallback to the old look if a font file ever fails. (2) **Consistency**: the prototype screen was a completely different product (dark navy `#1a1a2e` + system fonts) — restyled to the pop-art system (cream canvas, black borders, blue primary; layout untouched); designer + editor canvases unified from `#FFF9C4` to the `#FFFDE7` the rest of the chrome uses (deeper cream stays for accent fills); h1 letter-spacing eased for the wider Archivo Black. (3) **Emoji cleanup** (decorative text only — functional UI marks like ♥ favorites, 💬 feedback, goal chips, 🕊 badge, medals, and student avatars stay): stripped all emojis from game-config and recipe *text* (28 files — announce messages, prompts, templates; recipe `icon` fields kept as the picker's icon slot; ✓ and → kept as typography; whose-eyes' 💭/❓ field markers became "They feel:"/"They would ask:" labels), and from screen copy sentences (inbox zero 🎉, thanks 💛, owner banner 👑). Every 🎯 is gone (the goal-tag card prefix removed; recipe-icon fallback/placeholder now 🧩; estimate picker icon now 🔢; player points line de-emojied), and the last 📱 references died (QR buttons are plain text; teacher-device notices use 💻 per the Chromebooks-not-phones rule). Verified: 890 tests green, vocab-match universal sim end-to-end, screenshot review of home/library/designer/editor/host/player/teacher/prototype.
- 2026-07-28: **Library cards can Customize** — every library card now carries an edit affordance (user request: customization is the point). Own activities (and owner mode) get direct **Edit**; built-ins get **Customize** — one click clones the activity into a device-scoped copy ("<name> (my version)", featured flag stripped), registers it in MyGames/Recents, and opens the editor on the copy. Honest by construction: saving over a shared built-in stays owner-only, so the public path edits a copy instead of hitting a 401. Flow-verified live (copy created as a user game, editor opened).
- 2026-07-28: **Library-first Phase 2 — teachers pick prompts, they don't write them** (workstream B of LIBRARY-FIRST-PLAN.md; "the prompts are the intervention"). (1) **The Along corpus is ingested**: a one-shot parser turned the 57-page PDF into `recipes/prompt-banks/along.json` — 142 research-aligned prompts across 11 decks (fun & favorites, gratitude, conversation starters from students, a day in the life, imagine if, prior learning by subject, belonging, rigorous thinking, relevance & motivation, agency & choice, building blocks), **every prompt carrying its author** (CZI/Gradient Learning, Transcend, Black Teacher Collaborative, Search Institute, Character Lab, Re-imagining Migration), 84 with ready-made 4-way answer choices. The 9 Wellbeing questions are deliberately held back until the teacher-only-visibility default exists (noted in the bank file). (2) **`recipes/prompt-banks/lanyard.json`** — original decks: 12 Whose Eyes? perspective topics, 12 Both Sides debate claims, 5 Someone's Got You openers. (3) **`promptDeck` param type** — compiles exactly like a string (zero compiler risk); spec adds `bank` + `decks` + optional `choicesParam`; recognized by schema allowlist/validation/synthetic-params. (4) **The deck picker** — promptDeck params render a textarea + "🎴 Pick from the deck"; the Dialog-based picker shows each deck with description, prompt cards with attribution and "N answer choices included"; picking fills the text (editable after) and, via `choicesParam`, the poll's choices rows too. Served by `GET /api/prompt-banks/:id` (id-sanitized, 400/404 verified). (5) **Five recipes wired**: snowball, class-poll (with choices auto-fill), someones-got-you, whose-eyes, both-sides-rope — all keep plain-string defaults so zero-prep hosting is unchanged. New drift-guard test file: bank shape, attribution on every Along prompt, the wellbeing exclusion, and recipe↔deck reference integrity (a renamed deck fails the suite). Screenshot-verified picker inside Snowball's form. **Prompt count: 261** (142 Along + 90 Closer + 29 original) — counted here per the plan's "count prompts like tests" rule. Test count: **890** (+6).
- 2026-07-28: **Library-first Phase 1 — the library is the front door** (from docs/LIBRARY-FIRST-PLAN.md; nothing removed, everything re-doored). (1) **`/library`** — the new teacher-facing home: "What do you want to do together?", search + goal chips (Connect first), run-focused cards (big ▶ HOST THIS, Try it, ♥ favorites, Recently used section, no-winner badges, playTime/classSize); Edit appears only on activities made on this device; no delete/star/recipes — those stay in the designer. Reuses the designer stylesheet + shared modules (game-visibility, my-games, owner-mode, dialog) with Favorites/Recents extracted to `screens/shared/activity-prefs.js` so both surfaces agree. (2) **Home** — one primary card ("Find an Activity" → /library, Chromebook copy); the designer moved to a footer link. `/designer`, `/host` (dropdown and `?game=` deep links), everything else untouched. (3) **Builder doorway** — "🛠 Build your own activity" on the library opens an accessible dialog (optional "what do you want to make?" note) → files a `builder-request` in the feedback inbox (new category, "🛠 Wants to build" label) → opens the designer. Signal-only: typing /designer directly always works. (4) **`activity_runs`** — the runs-not-builds gauge: new Neon table (game_id, player_count, started_at — no names/content/codes), recorded once per room at start-game (never simulated rooms), owner-gated `GET /api/activity-runs` (per-game totals + last-7-days). Live-verified: /library 200, builder-request lands with its category intact, run recorded on a real start, endpoint 401s without the owner password; screenshot-reviewed home/library/doorway. Test count: **884** (+2).
- 2026-07-28: **Chromebooks, not phones — device-stance sweep + roadmap refresh.** Phones are banned in schools, so nothing student-facing may assume one. Swept: teacher-console pitch is now "💻 Teacher controls on a second device" (host chip, reveal instruction, /teacher join hint, wrong-PIN message — a laptop or spare Chromebook, not 📱 your phone); Whose Eyes? default topic changed from the now-awkward "school's phone policy" to "school's homework policy" (config + recipe); comments/docs updated (player client is "Chromebook / tablet", touch support framed as touchscreen Chromebooks, ARCHITECTURE diagram + console description, demo-room described as second-device testing). Deliberate keeps: PII "phone number" scrub patterns, the Telephone game reference in Idea Chain, bot-brain's "Phones away during discussions" class-norms answer (it AGREES with the ban), and CHANGELOG history. NEXT-STEPS "START HERE" rebuilt for 2026-07-28: everything through the chain primitives is deployed; what remains is proxy playtests (July window nearly closed), three teacher-only actions (PZ email, compliance facts, Render Starter), day-one kit, match/sort/drawing recipes, and the July loose ends. New standing rule recorded: student device = Chromebook; teacher console = "a second device."
- 2026-07-27: **Chain primitives — write, pass, add one, get it back** (COMPARATIVE-ADVANTAGE §7 asks 1-3). (1) **`prefillFromAssigned` on collect** — with rotateFrom, the classmate's item lands IN the recipient's text box so they add to it (accumulating lists, the +1-routine move); reconnect-safe; paired with a new **`maxLength`** collect field (default 280 stands; accumulating chains raise it) threaded through server validation (`checkSubmission`) and the player counter/maxlength. (2) **Return-to-author reveal** — `reveal scope:"own"` + `chainFrom:[...]`: every rotation chain returns to the student who started it. The rotation now stores `assignedFrom` (recipient→sender links) on the source phase, and pure `engine/phases/chain-reveal.js` walks the links (`buildChainViews`) — link-based, not index-replay, so reconnect id-migration keeps chains intact (chaos-verified: 3 mid-game reconnects, chains still resolved). `chainDisplay: steps|final` for chain-poem vs accumulating-list rendering; projector shows only a neutral line (chains are private to their authors). Live-probed: 3 players, each got back exactly their own list with both classmates' additions. (3) **`merge groupSize:3`** — trios, the canonical consulting-protocol size (n%3===2 → one pair; n%3===1 → the leftover joins a trio; never a singleton). New game #31 + 18th recipe: **One More Thing** — from-memory recall → two pass-and-add hops → your list comes back grown → AI builds the class's collective memory. Verified: 13 new unit tests (chain walker, trio grouping), universal sim, chaos run, scamper/snowball/finish-my-drawing regression sims. Test count: **882**.
- 2026-07-27: **The thesis slate — the front door now argues what the engine argues** (from docs/COMPARATIVE-ADVANTAGE.md + the DeKoven/New Games and Project Zero research sweeps; the diagnosis: every distinctive primitive was used exactly once while `leaderboard` appeared 18×, so the library said "nicer Kahoot" while the engine said addressed/chained/shared-fate). Three new activities, each config + recipe with a zero-prep default (the Along lesson: must beat a paper card on friction), all universal-sim verified: (1) **Whose Eyes?** — the class names everyone a topic touches, then each student steps inside a DIFFERENT classmate's suggestion (rotateFrom + 4-field collect + reveal-one circle) — 30 perspectives instead of the 5 a discussion gets; (2) **Someone's Got You** — everyone shares one small real thing (passAllowed), an assigned classmate writes one line of genuine encouragement, the teacher gates every line (preview) before the reveal-one wall; `family:"connection"`, so scores are impossible by validator; (3) **Both Sides of the Rope** — stance vote → evidence for BOTH sides → AI-sorted rope → "what if?" questions → re-vote → **the delta reveal** (`{{stance1.barChart}}` vs `{{stance2.barChart}}` — the class watches its own mind move; validator caught the raw `{{whatif.responses}}` and demanded `.list`, exactly as designed). Shopfront: **featured set rebalanced to the thesis-forward 10** (new three + one-voice/snowball/weekend-poem/art-gallery/speed-quiz/vocab-match/group-work-day; story-quest + who-said-it unfeatured), **every built-in config now carries goal tags** (20 more tagged — chips previously saw only 14 of 36), **"🕊 No scores, no winners" badge** on connection-family cards (`family` surfaced through /api/games; the validator's promise made visible), home tagline leads with the verb ("the class's own words become the next five minutes"). Recipes 14 → **17**. All names/prompts are Lanyard-original per the licensing gate (methods borrowed, expression ours; PZ email still owed before public marketing). 869 tests green.
- 2026-07-27: **Second UI review closed out — accessible modals, honest pre-room host, calmer editor header** — (1) **Accessible modals**: new `screens/shared/dialog.js` gives the overlay/modal pattern real dialog semantics — role=dialog + aria-modal + label, a visible ×, Escape-to-close, a live focus trap (focusables re-queried per keypress, so multi-view modals that swap content keep working), focus restored on close, backdrop clicks handled centrally, and a self-cleaning key listener for flows that navigate away. All four designer pickers retrofitted (template, recipe, legacy AI generator, from-description); `clearModal()` preserves the × across view swaps; template cards became real `<button>`s and recipe cards (which nest a delete control) got role=button + tab stop + Enter/Space. Verified with a new `scripts/screenshot-eval.js` (CDP screenshot after running page JS — click the link, shoot the modal, press Escape, shoot again). (2) **Pre-room host screen**: roster + Start hidden until a room exists; "Create Room" → "Open the room"; the selected activity's description shows under the dropdown; and the reviewer's "disabled but looks active" Start button was a real CSS bug — the `#start-game-btn` ID selector's green beat `button:disabled`'s gray (explicit `:disabled` rules added for start/close/end). (3) **Editor header**: Ask AI + Save as Recipe parked in a "More ▾" menu (same button ids, so all wiring untouched; `[hidden]{display:none}` guard per the recurring-bug list); "Saved" pill became quiet status text with header-contrast tints; hierarchy now Save > Check for Errors / Prototype Mode > More. (4) **Polish**: owner link renamed "Show full library (site owner)"; goal chips show result counts ("🤝 Connect 2"); playTime/classSize filled in on all 9 featured activities. 869 tests green; screenshot-verified (recipe modal open/Esc-closed, pre-room host, chips with counts, header menu open).
- 2026-07-27: **Teacher console wave — the console can actually run the class now** (top item of the second external UI review). The headline was a real bug with a tidy root cause: the console's very first "Next step" (and any pre-start action) was silently swallowed because the join snapshot reported `phaseInstanceId: 0` while a lobby room's counter is still `undefined` — the stale-event guard read the mismatch as staleness and dropped it. Fix: no instance id yet → nothing can be stale (`isStalePhaseEvent`). On top of that: (1) **Lobby state** — the console now shows a live "N students joined" count + name list (new `teacher-roster` event on join/leave/grace-expiry; snapshot seeds it) and an explicit **Start activity** button (start-game was already console-gated correctly); the generic next-step is hidden in the lobby so an accidental tap can't start the class. (2) **Descriptive step control** — the console's advance button reuses wave 2's continue-labels ("Send the question to students ▸") via `continueLabel` on `teacher-phase` + the snapshot. (3) **Pairing is visible** — every console join announces itself: "📱 Teacher device connected" chip on the host screen (with device count) and a warning line on already-connected consoles ("if that wasn't you, a student may have the PIN"). (4) **PIN reveal hardened** — the projector chip is now a two-step confirm ("⚠ Students may see this — tap again"), auto-hides after 15s (or 6s if unconfirmed), and hides immediately once a console pairs. Verified by a 7-assertion live probe (roster, labels, console-started game, dual-device notices), the full teacher-console invariant sim, and the complete chaos suite (47 genuinely-stale events still dropped). 868 tests green.
- 2026-07-26: **UI review wave 2 — a library you can actually find things in, and buttons that say what they do** — (1) **Library controls** on the designer grid: search (name/description/tags), goal chips built from a fixed teacher-intent vocabulary (🤝 Connect / 🎨 Create / 💬 Discuss / 🗳️ Decide / 🪞 Reflect / ⚡ Energize / 📚 Review — only goals present in the visible list render; seeded as plain `tags` into ~14 configs so recipes/AI matching can reuse them), ♥ favorites and a "Recently used" section (any card action files it; cap 8) — both localStorage, the same no-accounts model as MyGames. Sections render each activity exactly once: Favorites → Recently used → My Activities → Built-in. Delete visibility now computed per game (own creations always, built-ins owner-only) instead of per section. One render entry point (`refreshLibrary()`) replaces the four scattered `renderGames(applyVisibility(...))` call sites. (2) **Descriptive continue buttons**: announce and reveal host buttons now say what happens NEXT — "Send the question to students", "Start the voting", "Start counting together" — via pure `engine/phases/continue-labels.js` (label map keyed by next phase type, "Continue" fallback, virtual foreach sub-phases covered because they're injected into the same phase map). Live-probed (one-voice intro announce carries "Start counting together") + story-quest/snowball/weekend-poem sims + dedicated Snowball invariant sim all green. Test count: **868** (+4).
- 2026-07-26: **UI review wave 1 — the projector never lies about participation again** — an external UI review of the live site found one real runtime bug and a cluster of first-touch UX problems. (1) **"0 of 0 submitted" fixed**: collect and collect-choice were the ONLY phase types whose phase-start emit carried no count/total (every later phase — rank/match/sort/estimate/rate/wager/vote — already seeded `totalX`), so the projector read "0 of 0" until the first answer arrived. Both handlers now send `count: 0, total` using the same eligibility math as the submit handler (foreach author self-exclusion, unpaired-player exclusion); live-probed (0 of 2 before any submission on art-gallery) + Closer sim + universal sims green. (2) **Activity cards are actionable**: the designer grid's cards were clickable divs whose only visible action was Delete and whose click meant "edit" — the wrong default for "I have 10 minutes, run this." Cards now carry real link/button actions — primary ▶ HOST (`/host?game=` auto-creates the room), Edit, Try it (prototype), Delete (own/owner only) — keyboard-focusable with visible focus rings; card-level click removed (no nested-interactive a11y trap). (3) **Home hierarchy**: hosting is the dominant card; building demoted. (4) **Player join form**: permanent ROOM CODE / YOUR NAME labels replace the clipped 46-char placeholder; placeholders grayed. (5) **Teacher console discoverability** (the reviewer toured the whole site and never found /teacher): host-screen chip relabeled "📱 Teacher controls on your phone" with the full open-this-URL + room + PIN instruction on tap, and a Teacher Console link in the home footer. (6) **a11y**: the 🔊 chip announces "Turn sound effects on/off". Waves 2-3 (library search/recents, descriptive continue labels, goal chips, AI storyboard, pinned controls, collective visualizations) captured in NEXT-STEPS §1.2. 864 tests green.
- 2026-07-26: **Curated public front door + owner mode + site feedback** — the site now has two faces. PUBLIC: visitors see a curated set of 8 featured activities (variety pack: speed-quiz, vocab-match, snowball, one-voice, story-quest, art-gallery, group-work-day, who-said-it — `featured: true` in each config, surfaced through both `/api/games` and the socket `games-list`) plus anything created on their own device (`screens/shared/my-games.js` remembers created ids in localStorage; every save/create path registers). One pure filter (`screens/shared/game-visibility.js`, tested) drives all three pickers — designer grid, host select, prototype — with `?game=` deep links still working for hidden activities. OWNER: `SITE_PASSWORD` was REPURPOSED from "lock all teacher surfaces" to the owner's password — the old broad gate is gone (the public hosting/building IS the product decision), and it now guards only the feedback inbox, `/api/owner-check`, and edits/deletes of built-in activities (PUT/DELETE on a built-in without it → 401; visitor-created activities stay freely editable). "Site owner? Show everything" on the designer grid unlocks owner mode (basic-auth prompt once, browser caches the credential), revealing every activity + per-card ★ featured toggles + delete buttons on built-ins + a link to the inbox. FEEDBACK: floating 💬 widget (home/designer/editor/prototype; createElement/textContent only — scanner-proof) posts anonymous feedback (category + message, no name/email fields BY DESIGN) to `POST /api/feedback` — validated + content-filtered (`engine/feedback-validate.js`), per-IP rate-limited 5/min (`engine/simple-rate-limit.js`, pure + injected clock), stored in a new Neon `feedback` table (ndjson file fallback for local dev). Owner inbox at `/feedback`: New/Done/All tabs, mark-done/reopen. Live-verified end-to-end with SITE_PASSWORD set: featured list correct, POST open, GET/PATCH/inbox/owner-check 401 without password + working with it, built-in PUT/DELETE blocked for the public, /host + /designer publicly reachable, rate limit trips on the 6th post; screenshot-reviewed home/designer/inbox (which caught: public built-in cards showed Delete — now owner-only). XSS scanner extended to the 5 new screen files. Test count: **864** (+29).
- 2026-07-19: **PII scrub at the outbound AI boundary** — the last pre-August compliance code item, built to the second reviewer's design (which correctly killed the naive "full-name scrubber": arbitrary name detection can't work; known-roster removal can). New pure module `engine/pii-scrub.js`: roster names removed case-insensitively on word boundaries (conditional boundaries so names like "A+" match; longest-first so "Mary Jo" beats "Mary"; over-redaction accepted — it degrades AI output slightly, never classroom display) + email/phone/URL pattern redaction. Applied inside `AIService` real paths to COPIES only — process() responses AND instructions (resolved {{tokens}} embed student text), and generateFakeResponses' style examples; every game-time call site (ai-process, ai-eliminate, foreach aiInject, legacy close-submissions) passes `rosterNames`. The classroom's own data is never mutated. Verified by a boundary test that stubs `_callClaude` and asserts nothing name/email/phone-shaped leaves the server (and that the original response object is untouched), 11 pure-module tests, and a real-API weekend-poem playthrough. Deliberately separate from content-filter.js — display gating and subprocessor minimization are different concerns. Test count: **835** (+13).
- 2026-07-19: **Journal lockdown + Save-as-Recipe durability** (both from the second independent review). (1) `GET /api/rooms/:code/journal` — public until now, with journal entries carrying student names and hide/kick events, reachable by anyone with the wall-projected room code — requires the room's teacher PIN (`?pin=`) or the site password via basic auth; live-verified 403/403/200. (2) **"Save as Recipe" survived only until the next deploy**: user recipes lived solely in `recipes/user/` and Render resets the filesystem on every push — a doc-cross-referencing catch (db.js had `user_games` but no recipes table). New `user_recipes` Neon table mirrors the games pattern (save/insert-if-absent/delete/list); `recipe-loader.loadAllRecipes` accepts injected DB recipe objects through the same validate + compatibility pipeline as files (DB wins over same-id files); startup migrates filesystem strays; local no-DB dev unchanged. Live-proven: recipe saved via API with NO file written, server restarted, fresh process loaded it from Neon (14 → 15 recipes) and delete round-tripped. +4 loader tests. Test count: **822**. Also corrected the built-in recipe count (14, not 13).
- 2026-07-19: **P0: XSS sweep — untrusted text can no longer execute on any screen** (found by the second independent review from documents alone; code-confirmed). Real holes: student name+text hit `innerHTML` UNESCAPED on the host preview responses list (one of two branches — the drawing branch escaped, the text branch did not), the reveal `renderResponses` list, the relay shared-result on BOTH host and player, and the turn scoreboard (custom team names); teacher/AI-generated config leaked through rate-scale labels (`${s.label}` in the bar chart + pie titles, host and player), estimate answer/unit, and the editor's foreach preview; AI output rendered raw in the designer's "beyond what the framework can do" panel. A relay answer of `<img src=x onerror=...>` executed on the projector and every student device. Fixes: all sinks escaped; host `escapeHtml` upgraded to cover quotes (attribute contexts); player.js and designer.js gained escapers (had none). Already-safe by construction: `applyTemplate` (textContent), reveal/announce content, vote options, reveal-one, checklist rows, the AI review panel. **Enforcement:** `tests/screens/xss-sinks.test.js` — a statement-level scanner (multi-line aware) over all seven screen files that fails the suite on any HTML-building statement with unescaped interpolation; escape-internally helpers recognized; audited-numeric statements allowlisted with justifications; includes a self-test so the scanner can't rot silently. New CLAUDE.md rule: student text, teacher config, and AI output are all untrusted for rendering. Test count: **818** (+8).
- 2026-07-19: **P0 fixes from the independent review — flow-control gating + log scrubbing** — (1) **Flow control is teacher-only now**: the review caught `start-game`/`close-submissions`/`close-voting`/`advance-phase`/`relay-finish-all` accepting emits from ANY socket ("middle schoolers find this"); a full audit found 14 ungated flow events (also retry/skip-phase, reveal-next, close-ranking/-matching/-rating/-wager, wager-resolve, end-game). All now check `isTeacherSocket`; player gameplay events (submits, taps, describer actions) stay open by design, and turn got-it/skip were confirmed to validate the describer internally. Console sim grew intruder-can't-start and intruder-can't-close/advance invariants; full chaos suite + preview/reveal-one playthrough green. (2) **Student content never reaches production logs**: new `engine/content-log.js` (`contentLog` no-ops unless `DEBUG_CONTENT=1`) gates AI instructions/inputs/outputs and head-to-head vote winners (student answer text); join/submit/disconnect/eliminate/winner logs now carry socket ids + counts instead of names — Render's log retention no longer stores student data beyond our own 6h TTL story. Both COMPLIANCE-TODO P0s checked off. 810 tests green.
- 2026-07-19: **COPPA/FERPA hardening — AI data minimization + PIN lockout** — the two "before any real classroom use" code items from the California student-data review. (1) **Student names never reach the Claude API**: `AIService._buildUserMessage` and ai-eliminate's playerList now send pseudonymous playerIds + answer text only; new `engine/ai-name-fill.js` deep-fills the REAL names into AI JSON output afterward (any object with a recognized `playerId` gets truthful `playerName`), so shipped games like feedback-coach-academy that render `{{_current.playerName}}` keep working — and a model-garbled name echo can no longer beat the registry spelling. Generator guidance updated so AI-built games follow the contract. Verified by unit tests (a name in the outbound message is a test failure) + a real-API weekend-poem playthrough. (2) **Teacher-PIN brute-force lockout**: `engine/pin-throttle.js` (pure, injected clock — adjudicateTap pattern): 5 wrong PINs in 10 min lock a room's console joins for 5 min, even for the correct PIN, keyed by room code so a fresh socket doesn't reset it; success clears; hourly sweep. simulate-teacher-console grew the invariant (attacker spam → correct-PIN-on-fresh-socket still bounced). Also recorded two architectural rules in CLAUDE.md: no student name in any outbound API payload; saved objects (configs/recipes) never contain student-generated content. Snapshot lifecycle needed no work — purge-on-end + 6h TTL already beat the advice's 24-48h ask. Remaining workstream (documents, console verifications, watch list incl. AB 1159) captured in NEXT-STEPS §1.5. Test count: **810** (+14).
- 2026-07-19: **"Activity" vocabulary — copy sweep** — Lanyard increasingly makes things that aren't games (checklist, critique, Connection Pack, consensus polls), and "game" was hiding them: a teacher needing a lab checklist would never click "Build a Game". User-facing copy now says **activity** — the word teachers already use in lesson plans — across home ("Host an Activity" / "Build an Activity", "Interactive activities for your class"), designer ("Activity Designer", "What do you want to DO with your class?", "My Activities", "Create Activity", "Activity Settings"), host ("Choose an activity", "Start!", "End Session", "That's a wrap!"), player ("Join In", "Waiting to start...", "That's a wrap — thanks for playing along!"), teacher console, prototype, and server socket/API messages. "Game" survives only where something genuinely is one (elimination-game examples, end-phase messages in game configs). Considered "interaction" (rejected: nobody's natural vocabulary — a designer's word, not a teacher's). Deliberately copy-level only: `games/`, `gameId`, `GameEngine`, event names, and API routes unchanged — zero functional churn, all 796 tests green, home + designer screenshot-verified. Follow-up candidates: validator messages still say `Game "x":` (test-string churn), recipe `kind: game|activity` tag for per-item verbs.
- 2026-07-19: **Checklist phase (28th type) — shared group to-do list with a live dashboard** — the first pure classroom-management phase: teacher writes a task list, the class splits into groups (any team-split method; omit `teamsFrom` for one list per student), every group works the same checklist on their devices while the projector shows per-group progress bars with a small celebration as each group finishes. Any member checks or un-checks any item (merge's shared-draft trust model); checks carry attribution ("✓ Alice") shown to the group and the teacher console, never the projector. Teacher console gets full per-group detail + tap-to-toggle on a group's behalf (snapshot on mid-phase join too). Close is a summary moment (like sort/match — host clicks Continue); output `results`/`resultsList` feed downstream templates ("Great work today! {{worktime.resultsList}}"). Zero AI calls, no scores. Pure rules TDD'd in `engine/phases/checklist-state.js` (12 tests); kind-guarded idempotent `closeChecklist` in the advance-phase routing; reconnect restores board/summary; robot playtest + universal sim + chaos all play it. Shipped WITH its recipe (`recipes/group-work.json`: tasks + group size + how-groups-form + optional work timer — `$if` drops the timer cleanly) so it's reachable from the idea-first front door on day one. New game: **Group Work Day** (`games/group-work-day/`), verified by universal sim, chaos, live player screenshot, and editor Simple-view screenshot. Bonus fix the screenshots surfaced: match/sort sections were missing from BOTH screens' section registries — they stayed visible under whatever phase came next (latent since July 6). Test count: **796** (+12).
- 2026-07-19: **Open team capacity — pre-existing classroom teams** — `capacity:"open"` on team-split removes choice mode's even-split spot caps. The gap it closes: a class with 7 real teams of 4 and 2 absences got capacities 4,4,4,4,4,3,3, so a present member of a "full" capped team was locked out of their own team. With open capacity, `state.capacities` is null — no bounce in `team-pick`, rosters carry `capacity/open: null`, and every screen hides the spot counts (player pick cards show just the team name, host choice view drops "N spots left", teacher-arrange shows `(n)` instead of `(n/cap)`); straggler auto-fill already handled null caps (emptiest team). Default stays `"even"` — caps are right when kids pick freely. Validator warns when open is set on random/balanced (ignored there); editor gets a "Team spots" select (choice mode only); Simple view reads "students join their own team (no size caps)"; AI generator taught the pattern. Third split added to `games/_team-modes` + 4 new sim invariants (`simulate-team-modes.js` now 18: no-caps rosters, five-on-one-team pile-on, 5/0 split, custom names) plus a live player screenshot. New pure tests for `resolveTeamPlan`/`buildTeamRosters` (capacities moved into the plan). Test count: **784** (+7).
- 2026-07-06: **Drawing input v1 — students draw instead of type** — `collect.inputType:"drawing"` swaps the text box for a drawing pad. The first deliberate break from "games are text-based": strokes, not images — drawings travel as `{strokes:[{points,color,width}]}` with 0-1 normalized coordinates, so one drawing renders on a phone pad, a projector gallery card, and a 160px moderation thumbnail, and reveals can ANIMATE the strokes replaying. Shared browser module `screens/shared/drawing.js` (pointer-events pad covering mouse/touch/stylus, 7-color palette, undo/clear that never remove inherited strokes, `Draw.scribble()` for bots); pure server validation `engine/drawing.js` (clamp coords/width/color, trim to caps — 400 strokes/6000 points — reject only empty/malformed; TDD, 13 tests). Composes with the existing engine: **reveal-one** becomes an animated gallery with ✏️-name captions; **rotateFrom** rotates the actual strokes (`byPlayerDrawing`/`assignedDrawing`) so a drawing source preloads onto the recipient's pad (continue-the-drawing) or displays read-only above a text box (caption mode); **preview** shows the teacher real thumbnails before the class sees anything; the **teacher console** moderation list renders thumbnails (hide/kick) since a "[drawing]" placeholder is unmoderatable. Safety framing: the content filter can't read pictures, so drawing games lean on attribution + preview + console thumbnails — both shipped games route through preview before the projector. AI steps can't read drawings (validator warns on wiring one into ai-process). Editor: "Students answer with" toggle + Simple-view "Students draw:"; AI generator taught the pattern. Two games: **Art Gallery** (draw → preview → animated gallery) and **Finish My Drawing** (draw → pass-and-continue ×2 → preview → gallery — every picture has three artists). Verified by universal sim (which this work also fixed: it listened for 'preview' instead of 'preview-content' and had NO reveal-one support — every preview/reveal-one game stalled silently), chaos on both games, and live screenshots (which caught the recurring display-vs-`[hidden]` CSS bug on the caption-preview canvas). v2 (live mural / tile-wall phase) deliberately deferred until the widget and safety patterns prove out. Test count: **777** (+13).
- 2026-07-06: **Sort phase (27th type) — place items into named buckets** — categorization as a first-class step (metaphor vs simile, fact vs opinion). Deliberate UI call: tap-to-assign (each item shows the buckets as toggle buttons; Submit unlocks when everything's placed) instead of the originally-imagined drag — dragging items across 2-4 bucket zones is miserable on a phone, and the cognition is identical. Two modes decided by the items themselves (estimate's poll-mode precedent): every item carries a correct bucket → graded (`pointsPerItem` each, scoreMap output for leaderboards); none do → consensus poll (distributions only). Mixed configs are a validation error. Close is the discussion moment: per-item class distributions with the correct bucket highlighted green + accuracy % on the projector; personal score + answer key (and perfect-sort confetti) on player devices; host clicks Continue. Items shuffled server-side (Fisher-Yates); timer auto-submits partial boards; pure scoring TDD'd in `engine/phases/sort-scoring.js` (16 tests); kind-guarded idempotent `closeSorting` in the advance-phase routing; reconnect restores board/waiting/results. Editor: buckets row-list + item rows with a correct-bucket dropdown ("no right answer" = consensus), Simple-view sentence, token/data-ref pickers, valid-out-of-the-box default step; AI generator taught the pattern. Simulator bots (robot playtest + chaos + universal sim) all play it. New game: **Metaphor or Simile?** (`games/metaphor-or-simile/` — graded round + Genius-vs-Chaos consensus round → leaderboard), verified by sim playthrough, chaos, and live board/results screenshots. Test count: **764** (+16).
- 2026-07-06: **Teams upgrade — group size, teacher-arranged, and student-picked teams** — team-split now sizes by `teamCount` OR `groupSize` (exactly one; `engine/phases/team-grouping.js` computes the count — 22 kids in groups of 4 → 4,4,4,4,3,3, an odd pair-class forms a triple, never a singleton). Two new interactive methods join random/balanced: **teacher** (host screen shows unassigned name chips + team cards — tap a name, tap a team, tap an assigned name to send it back; Confirm auto-fills the rest) and **choice** (students tap the group they want on their device — open spots only, full teams bounce just the tapper, switching allowed until close; all-placed auto-closes, Confirm sweeps stragglers into the emptiest teams). Both end in the standard TEAM_SPLIT reveal with the unchanged `{teams, playerTeam}` output, so turn/relay/leaderboard consume every method identically; random mode also swapped its biased sort-shuffle for Fisher-Yates. Privileged events (`team-assign`, `team-split-confirm`) require `isTeacherSocket`; a player emitting them is ignored (sim-verified). New pure module TDD'd (12 tests); interactive state is kind-guarded phaseState with an idempotent `closeTeamSplit` wired into advance-phase routing; reconnect restores the pick screen mid-choice. Verified by `scripts/simulate-team-modes.js` (14 invariants: capacities, full-team bounce, straggler auto-fill, assign/unassign round-trip, explicit-assignment survival, intruder rejection) against the hidden `games/_team-modes` fixture, plus universal-sim and chaos runs (and a charades-bowl chaos regression for random mode). Editor: method dropdown grew the two modes, "Size teams by" toggle swaps teamCount/groupSize inputs, Simple view reads "The class splits into groups of 4 — students pick their own spots." Test count: **748** (+14).
- 2026-07-06: **Match phase (26th type) — pair two lists, auto-scored** — students see the left column fixed and drag the right column into alignment (drag-to-SWAP, not splice-reorder, so fixing one row never breaks another; touch + mouse + arrow-button fallback). Config: `pairs` (literal `{left, right}` array, ≥2, unique texts both sides, warn >8), `pointsPerMatch` (default 10), timer auto-submits the current board. Close is an estimate-style discussion moment: correct pairs + per-pair class accuracy bars on the projector, personal "you matched X of Y" + full answer key on player devices (confetti on a perfect board); host clicks Continue. Output `scores` is a scoreMap — leaderboards sum it like any graded phase. Pure scoring in `engine/phases/match-scoring.js` (TDD, 14 tests); kind-guarded idempotent `closeMatching` wired into the advance-phase routing; reconnect restores board/waiting/results; right column dealt with Fisher-Yates + re-deal if it lands solved (a pre-solved board actually happened in testing). Simulator bots (robot playtest + chaos + universal sim) all play it; editor gets a pairs row widget, Simple-view sentence with inline pair editing, token/data-ref picker entries, and a works-out-of-the-box default step. New game: **Vocab Match** (`games/vocab-match/` — French vocab + inventors rounds → leaderboard), verified by full sim playthrough, chaos suite, and live screenshots. Two screenshot-caught fixes: the dealt-solved board, and `button { display:block }` overriding `[hidden]` (the recurring CSS bug — added `button[hidden]` override). Test count: **734** (+14).

- 2026-05-19: **Safety increment 2 — host moderation** — `engine/moderation.js` (pure helpers: `responseToText`, `hasSubmitted`, `isVisibleSubmission`, `buildSubmissionList`). New events `SUBMISSIONS_UPDATE` / `MODERATE_HIDE` / `MODERATE_KICK` / `KICKED`. Server: `emitSubmissionsUpdate()` pushes the live submission list (name + text + hidden) to the host on each submit; hide toggles `player.responseHidden` (excluded from AI input + reveal at close-submissions, reversible); kick removes the player, blocks rejoin via per-room `kickedTokens`, and notifies the kicked socket. Host UI: collapsible "🛡 Moderation (N)" panel on the collect screen with Hide/Unhide + Kick per row, plus a ✕ kick button in the lobby player list. Player: `kicked` handler clears identity (keeps token so the block sticks); manual join now resends the saved token (per-room, so harmless cross-room). Tests: `moderation.test.js`. Test count: **475** (+10).
- 2026-05-19: **Safety increment 1 — input safety pipeline** — `engine/content-filter.js` + `engine/blocklist.js`. `filterContent()` word-boundary blocklist match with leet-speak / repeated-letter / separator normalization (no Scunthorpe false positives); `validateResponse()` length/mash/prompt-copy; `checkSubmission()` gates string + multi-field object responses. Wired into `submit-response` (free-text collect only; rejects → `RESPONSE_REJECTED`, nothing stored). Player UX: char counter, 280 maxlength, min-length nudge, red rejection notice that preserves text. AI: `SAFETY_RULES` block (content-safety + anti-prompt-injection) appended to every game-run system prompt in `_processReal`. Tests: `content-filter.test.js`. Test count: **465** (+21). Scoped from `SAFETY-DESIGN.md`; remaining increments: anonymous mode, rate limiting / DoS limits, PII redaction, relay-text filtering.
- 2026-05-19: **YouTube video embed** — `video: "<youtube url>"` field on announce/reveal/collect/collect-choice (same 4 phases as `image`). `engine/video.js#resolveVideoEmbed()` parses watch/youtu.be/embed/shorts URLs (+ `t=`/`start=` offsets) into an embed URL; returns null for non-YouTube input. Host-only by design — renders in an `<iframe class="phase-video">` on the projector screen (added to each phase's `hostToggles` as `'video'`, NOT playerToggles). `showSection()` blanks all iframe srcs on transition so audio never bleeds across phases. Editor: plain URL text field after the image widget. AI generator/recipe matcher pick it up automatically (schema-driven prompt). Tests: `video-embed.test.js` (parser) + `video-field.test.js` (validator allow-list). Test count: **444** (+17).
- Phase 5: Designed game config format with 9 phase types
- Phase 5: Defined 6 AI task types with structured JSON outputs
- Phase 5: Created safety design with three-layer mitigations
- Phase 5: Added authoring guide with validation and testing tools
- Phase 5: Completed feasibility analysis — Corn Story requires 10 primitives
- Phase 5 COMPLETE: All 5 design documents finished
- Phase 6: Implemented game loader, config validation, game selection UI
- Phase 6: Implemented Mood Check game
- Phase 6: Implemented player elimination, hooks system, eliminate phase handler
- Phase 6: Implemented vote handlers (pick-one and head-to-head) and winner handler
- Phase 6: Wired phase handlers into server, updated UI for voting and elimination
- Phase 6: Created Corn Story config (16 phases, 3 rounds)
- Phase 6: Fixed AI elimination bugs (JSON preamble, playerIds vs text, deduplication)
- Phase 6: Added simulator script for automated playtesting
- Phase 6 COMPLETE: All 3 games playable, 141 tests passing
- Phase 7: Implemented preview phase (server handler, socket events, host UI)
- Phase 7: Implemented timers (countdown display, auto-submit on expiry, warning styling)
- Phase 7: Completed preview phase fields in game editor (content, showResponses, approveNext, rejectNext)
- Phase 7: Created Story Builder game using preview + timers
- Phase 8: Redesigned editor UI for non-coders — friendly names, icons, grouped sections, helper text, data ref dropdowns, phase picker modal, AI lane in screen info
- Phase 9: Enhanced server validation — type checks, required fields per type, enum values, timer range, data ref existence, returnResults mode
- Phase 9: Client-side editor validation — mirrors server logic, friendly error messages, unreachable phase warnings, validation panel UI
- Phase 9: Fixed Test Game button — saves dirty config first, opens /host?game={id} with auto-create room
- Phase 9: Host auto-selects game from ?game= URL param
- Phase 9: Visual countdown timers — SVG ring on host, progress bar on player
- Phase 9: Smooth phase transitions — CSS opacity fade on host and player screens
- Phase 9: Player reconnection — disconnect grace period (30s), auto-rejoin, state restoration (sendCurrentState)
- Phase 9: Player avatars — colored initial circles in host player list, disconnected player styling
- Phase 9: isDirty tracking in editor — prevents losing unsaved changes
- Phase 9 COMPLETE: 179 tests passing, editor validation + play polish + reconnection
- Phase 10: Fixed 6 editor bugs — preview defaults, delete re-linking, vote data ref, ai-process format
- Phase 10: Added prototype mode — iframe-based playtesting at /prototype
- Phase 10: Dynamic AI processing messages — task-specific text on host and player screens
- Phase 10: Test Game button now opens prototype mode (pre-selects game, user adjusts player count)
- Phase 10: 180 tests passing
- Phase 11: AI game review — light check (Haiku on save) + deep review (Sonnet on demand)
- Phase 11: Review panel UI with summary, clickable phase issues, inline badges on canvas
- Phase 11: `POST /api/games/review` endpoint, AIService.review() with model selection
- Phase 11: 188 tests passing
- Phase 12: Added 3 new phase types — `announce` (show a message), `collect-choice` (multiple choice), `ai-eliminate` (AI judges and eliminates)
- Phase 12: Fixed reveal backward-compat (only scans when no template), preview validation (content OR template), eliminate/winner auto-advance with pause
- Phase 12: Fixed AIService.process() to pass systemPrompt through to API — ai-eliminate was using wrong prompt
- Phase 12: Updated review prompts to describe all 12 phase types (was only 9)
- Phase 12: ai-eliminate stores `survivors` array for use as vote candidates
- Phase 12: 202 tests passing
- Phase 13: Implemented loop/round system — loopBack/loopCount config fields, getNextPhaseId server helper, versioned phase data, _loop template variables, editor UI with loop fields + visual indicators, validation
- Phase 13: 212 tests passing
- Phase 14: Fixed announce phase — added HTML sections, JS listeners, and timer support to both host and player screens
- Phase 14: Implemented screen control — `hostTemplate`/`playerTemplate` for custom text, `hostShow`/`playerShow` for toggling built-in UI elements
- Phase 14: Added `.screen-template` divs + `applyShow`/`applyTemplate` helpers to host and player JS
- Phase 14: Server resolves and passes screen control fields in all phase emits + reconnection
- Phase 14: Validation for hostShow/playerShow (per-type toggle sets), hostTemplate/playerTemplate (string check)
- Phase 14: Editor UI — "Screen Control (Optional)" section with template textareas + toggle checkboxes
- Phase 14: 223 tests passing
- 2026-05-03: Per-player AI primitive — `ai-process` accepts `perPlayer: true` (engine generates N items, stores `byPlayer:{pid:item}`); new `{{X.mine}}` template token resolves per-recipient in collect/collect-choice prompts, announce, and reveal. Host sees `(each student gets their own)` placeholder.
- 2026-05-03: Synthetic `.list` resolver — `{{X.list}}` renders an array as a numbered text list (1. item / 2. item / ...). Added to AI generator prompt as the right way to display arrays.
- 2026-05-03: Save endpoint auto-strips unknown fields before validation (mirror of AI generator strip pass). Stale invented fields no longer block save.
- 2026-05-03: Foreach pattern — added "Guess the right answer" alongside spot-the-lie. `_current.shuffledFields` now excludes prompt-style keys (question/prompt/scenario/topic).
- 2026-05-03: Simple Poll template now uses collect-choice + barChart to match its bar-graph icon.
- 2026-05-03: collect/collect-choice prompts now resolve `{{...}}` refs (latent bug — they were being passed through unresolved).
- 2026-05-03: collect-choice responses store both `choice` and `text` so AI summarize sees the answer.
- 2026-05-03: Ask AI modal — fixed `display:flex` overriding the `[hidden]` attribute (modal popped open every page load and ignored close button).
- 2026-05-03: Prototype Skip Timer button — clicks whichever advance/close button is currently visible on host via postMessage, bypassing iframe focus issues.
- 2026-05-03: **Server crash fix** — stale `setTimeout` auto-advances in announce/eliminate/ai-eliminate/leaderboard/winner now check `ctx.isStale()` before transitioning. Skipping past a timed phase used to leave a leftover timer that fired later and threw an invalid-transition exception, killing the process.
- 2026-05-03: **Synthetic suffix resolver generalized** — `.list`/`.barChart`/`.pieChart`/`.chart` now work at any nesting depth. `{{collect-id.responses.list}}` renders the responses array as a numbered list. Bare `{{X.barChart}}` still reads `X.tally` (backward compat). New `formatList` helper auto-pulls arrays from `.result`/`.responses`/`.standings`.
- 2026-05-03: **Validator: cycle detection** — DFS over `next`/`approveNext` (excluding `loopBack` and `rejectNext`, which are intentional back-edges) flags any unintended loop as an error. Caught a real bug in corn-story (stub `phase-16` looping back into round1).
- 2026-05-03: **Validator: raw-array template warning** — scans `template`/`content`/`message`/`prompt`/`instruction`/`hostTemplate`/`playerTemplate` for `{{X.field}}` where field is a known-array (`responses`, `standings`, `eliminated`, `survivors`, `winnerIds`/`Names`, `rankings`, `matchups`, `candidateIds`, `voters`) without a `.list`/chart suffix. Warns to add `.list`. Caught 3 real bugs in feedback-coach-academy on first run.
- 2026-05-03: **Validator: design-hole warnings** — `scanForDesignHoles()` warns on (a) `wager` with no `scoresFrom` and no `correctOption` (host has to pick winner manually, players bet on nothing meaningful), (b) `team-split` with no downstream template referencing team data (wasted setup).
- 2026-05-03: **Wager default starting pool** — handler now gives every eligible player 100 points when `scoresFrom` isn't configured. Simplest case (one-off bet, no prior score chain) just works.
- 2026-05-03: **Corn Story rewrite** — deleted broken stub `phase-16`, repointed `crown.next → end`, fixed lobby/intro/collect ordering (intro now precedes collect), added 3 "Here's what everyone said" reveals using `{{X.responses.list}}`. 19 phases (was 16). Test asserting count updated.
- 2026-05-03: **Dream Vacation Debate rewrite** — dropped unused `team-split` and `wager` (no points basis, no objective winner — teams collaborated on one shared pitch, so "which team won?" had no answer). Flow now: lobby → submit → show-suggestions → rank → announce-winner (full ranking) → relay → pitch-reveal → end. Bumped announce timer 8s → 20s. Relay prompt now references `{{rank-destinations.rankings.0.item}}` so players have inline context.
- 2026-05-03: **Relay phase fixes**:
  - Player UI: added `#relay-prompt-display` element. Players now see the prompt above the running text on both their turn and while waiting.
  - Relay handler now resolves `{{...}}` refs in prompt via `ctx.resolveTemplate()` (latent bug — prompt was passed through raw).
  - Server emits `prompt` in `relay-waiting` event (was only sent on `relay-turn`).
  - New `relay-finish-all` socket event — host can fast-forward all remaining turns. Each remaining player gets `(skipped)`, then phase advances.
  - Host's `prototype-skip` handler detects when `relay-section` is visible and emits `relay-finish-all` (relay has no host-side button).
  - Prototype Bot Fill now repeats 12× over ~7s so each rotating active player gets filled in sequence.
- 2026-05-03: **Bot-fill regression fix** — earlier same-day change made bot-fill loop unconditionally for ~7s. That auto-skipped the next phase (e.g. vote screen never visible — players auto-voted before the screen rendered). Loop now only continues while the host iframe's `relay-section` is visible; non-relay phases get a single shot. Cap raised to 25 shots (~15s) for the relay path. Same-origin DOM read used to check host state; cross-origin fallback is single-shot.
- 2026-05-03: **Validator: unreachable-phase detection** — BFS from lobby across `next`/`approveNext`/`rejectNext`/`loopBack`. Any orphan phase warns (e.g. `winner` with no `next` strands `end`). Editor had this client-side but server didn't — now it does, so saved-via-API configs can't slip through. `detectUnreachablePhases()` in `engine/game-loader.js`.
- 2026-05-03: **Last One Standing rewrite** (`games/elimination-game/config.json`) — was 1 round, called itself "Last One Standing" but crowned a winner immediately. Now: 3-round loop on `eliminate.loopBack: round-intro, loopCount: 3`. Added `show-answers` reveal between collect and vote. `voting.voters: "remaining"` so eliminated players don't vote in later rounds. Added `winner.next: "end"` (caught by the new unreachable check). Round intro shows `Round X of Y` via `{{_loop.eliminate.iteration}}`.
- 2026-05-03: **ARCHITECTURE.md rewrite** (`docs/ARCHITECTURE.md`) — replaced the stale doc (claimed 2,376-line server.js, 900-line handlePhase, 264 tests, separate voteState/rankState properties — none current). Now reflects: registry-based phase handlers (`engine/phase-handlers/`), `phase-context.js` API, `room.phaseState` single bag with auto-cleanup, `phaseInstanceId` staleness checks, journal ring buffer, paused-error recovery, validator pipeline (errors + warnings), 281 tests, full editor→save→run lifecycle, current REST endpoints (7 AI-related + CRUD).
- 2026-05-05: **Phase D — AI generator reads from schema** (`services/ai-service.js`) — new `buildPhaseDocsForPrompt({format})` derives field listings for both LIGHT_REVIEW_PROMPT (terse one-line per type) and GAME_GENERATOR_PROMPT (verbose with field types) directly from `PHASE_SCHEMAS`. Hand-curated prose preserved via `PHASE_EXTRA_GUIDANCE` map (multi-field collect, per-player AI, foreach guidance). Adding a new phase to the schema now flows automatically into the AI prompts.
- 2026-05-05: **Phase E — resolver grammar + typed dataflow** (`engine/resolver-grammar.js`, new) — single source of truth for `{{...}}` syntax. Four exports: `parseTemplateTokens` (find tokens in a string), `parseRef` (structural parse: kind/segments/suffix), `classifyRef` (schema-aware: producedType/capability/renderable/canonicalForm/problem), `checkDataRefCompat` (does ref satisfy `accepts: [{type, capability}]`?). Validator and engine both run refs through this module so they can't drift on what's a valid token. `KNOWN_SUFFIXES = {list, count, json, barChart, pieChart, chart, mine}`. `BUILTIN_SCOPES = {remaining, eliminated, players, _current, _foreach, _candidates, _loop}`.
- 2026-05-05: **Validator — schema-driven raw-array scan** — `scanTemplatesForRawArrays` rewritten to use `parseTemplateTokens` + `classifyRef`. Replaces hardcoded `KNOWN_ARRAY_FIELDS` set; typing now flows from `phase-schemas.js` outputs. Adding a new array output to a schema participates automatically.
- 2026-05-05: **Validator — typed-dataflow scan** — new `scanDataRefTypeMismatches` walks every `dataRef` field on every phase, looks up the producer's output type, and emits `DATA_REF_TYPE_MISMATCH` warnings via `checkDataRefCompat`. Surfaces silent type drift (e.g. wiring a leaderboard to `ai-process.result` when `format: json` returns an object — would render as empty leaderboard at runtime). Caught real bugs in feedback-academy, feedback-coach-academy, mad-lib-mashup on first run.
- 2026-05-05: **Engine resolver uses parseRef** — `engine/game-engine.js#resolve()` now dispatches via `parseRef` rather than its own ad-hoc string split. Validator and engine share one grammar; behavior identical (47 game-engine tests still pass). Renderable suffixes (`list/barChart/pieChart/chart`) handled in engine; `mine` is rewritten by server-side per-player helper before resolve runs; `count/json` are validator-only annotations.
- 2026-05-05: **Schema fix — vote/rank candidates accept responseArray** — `vote.candidates` and `rank.candidates` were declared `accepts: [candidateSource]` only, but voting/ranking on raw collected responses is a primary use case. Now both also accept `responseArray`. Removed false-positive DATA_REF_TYPE_MISMATCH warnings on corn-story and dream-vacation.
- 2026-05-05: **Test count: 298** (`npm test`). New diagnostic-codes covered: `DATA_REF_TYPE_MISMATCH` fixture in `tests/engine/validator-diagnostics.test.js`. Snapshots updated for feedback-academy, feedback-coach-academy, mad-lib-mashup to reflect newly-surfaced bugs.
- 2026-05-06: **Recipe layer R1 (foundation)** — `engine/recipe-schema.js` (Recipe shape, validateRecipe, validateParams returning structured Diagnostics), `engine/recipe-compiler.js` (coerce form-strings → declared types, apply defaults, substitute `${param}` placeholders with whole-value type preservation), `engine/recipe-loader.js` (scans `recipes/` at startup, caches in memory, separate `recipes/user/` path for R5). First recipe `recipes/class-poll.json`. New endpoints: `GET /api/recipes`, `GET /api/recipes/:id`, `POST /api/recipes/:id/compile`. Compiled configs run through `validate()` as a safety net. Test count: **368** (+70). Plan in `docs/RECIPE-LAYER.md`.
- 2026-05-06: **Recipe layer R2 (picker UI)** — Designer landing page now has a primary "⭐ Use a Recipe" button. `screens/designer/designer.js` adds `showRecipePicker()`, parameter-form auto-renderer (one widget per type: text/textarea/number/checkbox/select/array-repeater), submit flow (gather → compile → save → redirect to editor). Inline error display reads structured Diagnostics. Form-string coercion handled server-side by the compiler ("30" → 30 for integer fields). CSS in `screens/designer/styles.css`. The phase-graph editor stays as the post-creation editing surface.
- 2026-05-06: **Recipe layer R3 (seed library)** — Five new hand-built recipes shipped: `discussion-starter` (collect → AI summarize themes → reveal), `creative-vote` (collect → vote pick-one → winner), `elimination-tournament` (round-intro → collect → vote → eliminate bottom-% → loop → winner; uses `loopBack`/`loopCount` driven by recipe param), `anonymous-feedback` (intro about anonymity → collect → AI paraphrase themes → reveal), `story-builder` (relay turns → reveal `{{story.text}}`). All recipes auto-validated by `tests/engine/recipes-integration.test.js` — every recipe must compile cleanly with sample params and produce a config that passes `validate()`. **6 recipes total** in the picker. `quiz-show` deferred — needs compiler extension for array-driven phase generation; documented in `docs/RECIPE-LAYER.md` as future R3.5.
- 2026-05-06: **Recipe layer R4 (AI as recipe matcher)** — `aiService.matchRecipe(description, recipes)` calls Haiku with a system prompt listing every available recipe + its parameters. Output is a tiny structured object: `{recipe: 'id', params: {...}}` or `{noMatch: true, reason, suggestion}`. New endpoint `POST /api/games/from-description`. Designer's "AI Generate Game" now uses this flow: description → AI matches recipe → preview the recipe + filled params → confirm → save + redirect. The legacy whole-config Sonnet generator is preserved as `showLegacyAIGenerateModal` (renamed) and reachable from the no-match view as "Generate Custom (Advanced)". **This resolves the JSON malformation bug class** — AI emits ~5 fields, not 200 lines, so parse errors at character 14640 stop happening. Cost: ~10× cheaper per generation (Haiku, 800 max-tokens vs Sonnet, 4096 max-tokens + auto-polish review).
- 2026-05-07: **Recipe layer R5 (save-as-recipe)** — `engine/recipe-extractor.js` adds `extractCandidates(config)` (walks every phase, returns all parameterizable fields with auto-suggested names + labels; collision-aware naming) and `buildUserRecipe(config, paramSpecs, metadata)` (substitutes `${name}` placeholders, preserves enum.values/array.item/integer.min-max/string.minLength-maxLength from the source phase schema, validates). New endpoints `POST /api/recipes/draft` (returns candidates) and `POST /api/recipes/user` (builds + smoke-tests + saves to `recipes/user/{id}.json`, busts cache). Editor toolbar adds "⭐ Save as Recipe" button → 2-step modal (candidate checklist with editable name+label per row, then recipe metadata). Round-trip property tested: game → extract → build → compile produces the same game back. Built-in id collisions blocked. Test count: **389** (+21).
- 2026-05-07: **Recipe layer R6 (polish)** — compatibility check in `engine/recipe-loader.js` compiles each loaded recipe with synthetic defaults + runs the result through `validate()`; on failure, flags `_broken: true` + `_brokenReason` instead of silently dropping. `summarizeRecipe()` now exposes `source: 'built-in'|'user'`, `broken`, `brokenReason` so the picker can render warning badges + group sections. New `DELETE /api/recipes/user/:id` endpoint (refuses built-ins, busts cache). Designer picker now renders "My Recipes" + "Built-in Recipes" sections, with delete buttons on user cards and orange "⚠ Needs update" badges on broken cards (broken recipes sort to the bottom of their section). Test count: **392** (+3). **Recipe layer plan R1–R6 complete.**

- 2026-05-12: **Rotation primitive (SCAMPER)** — `collect.rotateFrom: "<phaseId>"` makes the engine assign each eligible player a different player's item from the named source phase. `{{<phaseId>.assigned}}` token renders that item per-recipient (resolved by `resolvePerPlayerTemplate`). The assignment map is stored on the SOURCE phase (`engine.phaseData[source].assigned`) so a downstream collect can chain by simply referencing the previous collect by id. Cyclic shift `((i - offset) % N + N) % N` guarantees no self-assignment and full coverage. New game `games/scamper/` (19 phases). Validator + grammar both extended with `.assigned` suffix. Schema-driven so the editor sidebar can expose it later. End-to-end verified by `scripts/simulate-scamper.js`. Test count: **405**.
- 2026-05-12: **Rate phase** — `engine/phase-handlers/rate.js`, schema entry, server `closeRating` helper, host + player UI. Phase takes a `scales` array of `{id, label, min, max, labels?}` objects and a `visibility: "all"|"host-only"` flag. Players see N button rows (one per scale), submit all at once. Server aggregates averages + per-scale distribution and emits `RATE_RESULTS`. Critically, the handler does NOT auto-advance — the host clicks Continue (advance-phase) so everyone has time to read the chart. Player must be brought back to rate-section on results event (they were pushed to waiting on submit). Results rendered as a horizontal averages bar chart (red→green hue interpolated from value/min/max) + per-scale conic-gradient pie charts with legend. First rate game: `games/class-critique/`.
- 2026-05-12: **Phase image uploads** — `image: "assets/foo.jpg"` field on announce/reveal/collect/collect-choice. New endpoint `POST /api/games/:gameId/assets` (multer, 5MB cap, JPEG/PNG/GIF/WebP only) sanitizes filenames and writes to `games/<id>/assets/`. Express now serves `/games/<id>/...` statically. Server-side `resolveImageUrl()` builds the runtime web path from the config-relative path. Editor sidebar widget on those 4 phase types: file input + thumbnail + remove + drag-and-drop zone with hover highlight. Host/player visibility controlled via the existing `hostShow`/`playerShow` toggles (`'image'` added to each phase's toggle vocab).
- 2026-05-12: **Auto-save + click-outside-collapse** — `deselectPhase` now awaits an `autoSaveIfDirty()` PUT before clearing the selection. A document-level click listener detects clicks outside `.phase-box` (and outside modals / validation / review panels) and triggers deselect. Saves silently; surfaces validation errors via the existing panel (and keeps the form open so user can fix). Brand-new games (no `gameId`) skip auto-save until first manual Save to avoid the ID prompt firing on every click-off.
- 2026-05-12: **Inline-form click bug fix** — clicks on text inputs in the inline-attached phase config bubbled to the phase-box's click handler, which called `selectPhase` → `renderPhaseConfig` and destroyed focus mid-edit. `handlePhaseClick` now short-circuits if the click target is inside `.phase-box-inline-form`. Number inputs masked the bug because the browser handled keyboard arrow input on the re-rendered focused field; text inputs lost characters entirely.
- 2026-05-12: **Designer-vibe default theme** — host + player JS now apply the `pop-art` theme preset on load (before socket connection), so even the lobby / room-code screens have the Keith Haring vibe (cream canvas, black borders, flat colors). Base `body` font bumped to `Arial Black` + 3px black-bordered inputs/buttons/prompts on both host and player CSS so the styling reads as bold even when no theme overrides apply. Game-specified themes still win — they're applied after on `room-created`.
- 2026-05-12: **Idea Chain recipe** — `recipes/idea-chain.json`. 4-round telephone using `rotateFrom`. Params: starter prompt, transform prompt (reused all 4 rounds), timer. Phase shape: lobby → intro → starter collect → 4× (announce + collect rotateFrom previous round) → reveal starters → reveal finals → end. Hand-coded 4 rounds because the compiler doesn't yet support array-driven phase generation (deferred along with quiz-show).
- 2026-05-12: **Class Critique recipe** — `recipes/class-critique.json`. Uses the rate phase. Params: topic, three scale labels (defaults Originality/Effectiveness/Feasibility — interpolated into the `scales` array via `${scale1Label}` string substitution), visibility, timer. All scales are 1-5 with no end labels; teachers can edit further in the editor afterward.
- 2026-05-12: **AI generator awareness of rotation + rate** — added `PHASE_EXTRA_GUIDANCE.rate` and a rotation block under `PHASE_EXTRA_GUIDANCE.collect` in `services/ai-service.js`. Includes concrete examples (chain of rotateFrom collects, rate scales array shape). The recipe matcher picks up new recipes automatically since recipes are passed in as the source of truth — confirmed live: "telephone game where each person sees the previous answer" matches `idea-chain`; "students rate a presentation on originality, clarity and impact" matches `class-critique` with the custom scale labels auto-extracted.
- 2026-05-12: **Render-ready** — `PORT = Number(process.env.PORT) || 3000` so Render's assigned port is honored. `package.json` gets `engines: {node: ">=18.0.0"}`. `.gitignore` extended for `recipes/user/`, `.env.*.local`, common OS/editor noise. Static-serve mount for `/games/<id>/...` already in place from the image upload work, which doubles as the runtime path for any per-game asset.
- 2026-05-12: **Test count: 427** (`npm test`). New test files: `rotate-from.test.js`, `rate-phase.test.js`, `image-field.test.js`. Snapshots auto-updated for the two new games (`scamper`, `class-critique` — both clean, empty diagnostic sets).

---

- 2026-05-13 → 2026-06-08 (condensed — this period went unlogged; see `git log` for detail):
  - **Bluffing primitives** — `collect.assign:"pairwise"`, `vote.matchupsFromPairs`/`excludeAuthors`, `collect-choice.choicePool`/`excludeAuthored`/`shuffle`. Games: Quiplash-style, Fibbage, Definition Bluff, Yes-or-No Bets (later renamed to avoid trademark conflicts).
  - **Speed-bonus scoring** — `collect-choice` with `correctAnswer` + `speedBonus`; Kahoot-style time-decay points in pure `engine/speed-scoring.js`. Game: Speed Quiz.
  - **Turn primitive + Charades Bowl** — Fishbowl-style describe/act/one-word rounds; server-authoritative per-turn timer, team rotation, Got It/Skip pool.
  - **Safety pipeline** — `engine/content-filter.js` + blocklist (leet-speak normalization) gating `submit-response`; AI safety-rules block on every game-run system prompt; host moderation panel (hide/kick) on collect phases.
  - **YouTube embeds** — `video:` field on announce/reveal/collect/collect-choice (host-only); `engine/video.js` URL parser.
  - **Neon Postgres persistence** — user-created games in `user_games` table (`db.js`); survives Render redeploys. Filesystem fallback when `DATABASE_URL` unset.
  - **Password gate** — `SITE_PASSWORD` env var → HTTP Basic Auth on /host, /designer, /prototype. Student paths stay open.
  - **Designer humanization series** — primary field inline on phase boxes, template tokens shown as friendly labels with live preview, "+ Insert from earlier step" picker, data-refs named by the actual question text, settings panel declutter, game categories.
  - **Rebrand: Classroom Games → Lanyard** — plus home screen redesign (two primary cards + student room-code join), STYLE-GUIDE.md, site-wide style alignment, emoji cleanup.
  - **Rank drag-and-drop** — HTML5 drag + touch; arrow buttons kept as fallback. Plus comma-separated string candidates accepted (AI generators emit that format).
  - **Prototype carousel view** — "one at a time" player navigation alongside Grid.
  - **Misc** — leaderboard ties share medals (Olympic ranking), `ai-process perPlayer` auto-retry on JSON parse failure, AI endpoints fail loudly when no API key, render.yaml.

- 2026-06-09: **Connection Pack** (commit 481dd4c) — first no-winner game family (spec: `docs/connection-pack-spec.md`). Three experiences as recipes + compiled games: **Closer** (escalating pair conversations, ~90 original prompts in 3 tiers), **Snowball** (think-pair-share), **One Voice** (cooperative counting). Two new phase types (22 + 23): **merge** (shared live draft, last-write-wins, `agreeMode` both/any/timer, quad-join) and **one-voice** (server-authoritative tap adjudication via pure `adjudicateTap` with injected clock; teacher-speaker Web Speech audio). Shared primitives: `passAllowed` (pass is never attributable anywhere), `simultaneousReveal` (nameless ticker), `reveal.scope:"pair"` + `{{_pair.*}}`, `family:"connection"` (validator permanently rejects winners/elimination/graded scoring), recipe `feel` tags. Pairing engine: greedy non-repeat matching, `oddHandling:"triple"`, `rotatePairsFrom`/`reusePairsFrom`. Zero AI calls in default paths.
- 2026-06-09: **Idea-first front door** (e4d31a4) — `/designer` opens with one big box ("What do you want to play with your class?") routing through from-description recipe matching. Example chips, `?idea=` deep-link, old three buttons demoted to links.
- 2026-06-09: **Robot playtest in "Check for Errors"** (e3ddbed) — deep review now also PLAYS the game: `services/simulator.js` self-connects 1 host + 4 bots in a hidden temp room (mock AI, zero spend), parallel with the Sonnet review. Catches stalls, crashes, blank screens, empty payloads, unresolved `{{tokens}}`, `[object Object]`. First run caught a real shipped bug (mood-check's `{{process.result.content}}`).
- 2026-06-09: **Teacher console** (a889418) — `/teacher` private second-device view; the host screen is PROJECTED, so "teacher-only" UI there was actually public. Room code + 4-digit PIN (click-to-reveal chip on host screen) or SITE_PASSWORD basic-auth. Live moderation with names, preview approve/reject, close/next controls. Privileged socket actions check `isTeacherSocket`.
- 2026-06-09: **Rank "My own list"** (9954424) — rank items can be a teacher-typed fixed list, not just an upstream data ref. Plus review-AI fix: stop suggesting "questions to ask" for fixed lists (b4b4a22).
- 2026-06-09: **Prototype carousel polish** (4 commits) — dots centered over the player panel with a dark pill; arrows flank the player panel only; chrome appears only in one-at-a-time mode and only after Launch.
- 2026-06-09: **Prompt-aware Bot Fill** (3f28610) — `screens/shared/bot-brain.js`: pure rules, no AI. Keyword banks, embedded-choice picking, yes/no detection, multi-field placeholder matching.
- 2026-06-09: **Pair reveals labeled honestly + hand-buildable** (ebea532) — "Show Everyone" → "Show Each Pair" when scoped; editor exposes "Who sees it" + pairing/privacy settings so the Closer pattern can be built by hand.
- 2026-06-09: **Simple view — the plain-English editor** (90b0b83 + 3 fix commits) — editor now DEFAULTS to a sentence-per-step view with inline editable boxes; phase canvas demoted to "Advanced". Polished via self-screenshot review (new workflow: launch server → headless screenshots → review own renders) and a 9-game sweep; "Advanced settings" deep-links to the expanded step.
- 2026-06-09: **Teacher console screenshot review + tooling** (5ea04e9) — header wrap fixed, count seeds on join, "Next step" hidden during preview (couldn't-skip-review), empty controls bar hidden. New permanent tools: `scripts/screenshot.js` (CDP headless screenshots — socket pages hang plain `--virtual-time-budget`) and `scripts/demo-room.js` (holds a live room with bots, prints CODE/PIN).
- 2026-06-09: **Test count: 642** (`npm test`).
- 2026-06-10: **Juice pack** (closes the 2026-06 strategic review) — `screens/shared/juice.js`: Web Audio synthesized SFX (no asset files; cues `pop`/`blip`/`reveal`/`tada`/`fanfare`/`womp`/`tick`, per-cue 150ms throttle, lazy AudioContext unlocked on first gesture), self-cleaning canvas confetti (colors read from the active theme's CSS vars; honors `prefers-reduced-motion`), deterministic emoji avatars (`avatarFor(name)` — same animal on host lobby, leaderboard, winner standings, team cards, and the student's own device), persisted mute toggle (🔊 chip top-right on host). Theme-level config: each preset theme declares `juice.wave` (pop-art triangle, arcade square, ocean/sunset sine) via `window.__themeJuice`. Host cues: join pop, submission/vote/rank/rate/wager blips, reveal chimes, leaderboard tada + staggered row pop-in, elimination womp, winner fanfare + confetti, one-voice success confetti, final-5-seconds timer ticks. Player devices stay quiet except their own moments: submit blip + bounce, personal confetti for winners / rank-1 / one-voice success, womp if eliminated. Every entry point guarded — juice failure can never break gameplay. Verified via lobby screenshot harness (host + 4 players). Tests: `juice.test.js`. Test count: **650** (+8).
- 2026-06-10: **No more visible {{code}} anywhere a teacher reads** — three-layer fix after user spotted `{{ai-mashup.list}}` in Simple view. (1) Simple view: token-aware text boxes — content with `{{refs}}` renders as editable text segments with friendly chips ("list of answers from step 5", "the winning answer from step 8", × to remove, hover for raw token); config stays byte-exact. (2) Advanced view: the canvas primary textarea now tokenizes refs to `[Winning answer — step 8]` labels (same tokenize/detokenize round-trip the screen-control fields use); `buildTemplateVariables` vocabulary expanded from 4 ref kinds to the full resolver surface (winner/list/barChart/tally/mine/assigned/rankedList/merged.list/one-voice stats/eliminated.length/remaining.length) with **step-unique labels** — fixes a latent corruption bug where two same-type steps shared a label ("AI result") and detokenize silently rewired refs to the wrong step; insert-menu items now insert friendly labels; `phaseContentLabel` strips tokens so step references in sentences never show braces. (3) Game sweep: full token inventory across all configs surfaced feedback-coach-academy showing students literal `{{_current.critique}}` (reveal-one resolves `message` once — no item scope) and raw JSON items. New `itemTemplate` field on reveal-one (`formatRevealItem` pure + tested) renders each item via `{{_current.field}}`; new validator rule `SPECIAL_SCOPE_OUT_OF_CONTEXT` warns whenever `_current`/`_foreach`/`_candidates`/`_pair` appear where they can't resolve ("students will see the raw code on screen"). Repaired feedback-coach-academy (itemTemplate) and feedback-academy (stale fields, `[object Object]` scenario prompt → `result.work`/`result.context`). Test count: **658** (+8).
- 2026-06-10: **AI cost guards** — `services/ai-budget.js`: every real Anthropic call now passes through `AIService._callClaude` (single choke point; all 10 `messages.create` sites converted), gated by two env-configurable guards: sliding per-minute throttle (`AI_CALLS_PER_MINUTE`, default 20) and daily cap (`AI_DAILY_CAP`, default 500; 0 disables either). Day counter persists in Neon (`ai_usage` table) when `DATABASE_URL` is set, so a redeploy/restart can't reset the cap — verified live. Blocked calls throw `AiBudgetError` (statusCode 429, teacher-readable message naming the env var + retry-after seconds) BEFORE reaching the API, so they cost nothing. Failure modes: editor endpoints return HTTP 429 with the friendly message; in-game ai-process surfaces through the existing phase-error pause (Retry works after the window clears); fake-responses/theme fall back to mock; auto-polish skips. Mock mode never reaches the guard. `GET /api/ai-budget` shows today's usage (async `peek()` loads the persisted count so it's truthful right after restart). Closes the "no app-level spend protection" gap (Anthropic Console limit was the only backstop). Tests: `ai-budget.test.js` (injected clock — throttle window slide, daily rollover, store resume, broken-store tolerance). Test count: **669** (+11).
- 2026-06-10: **Recipe compiler conditionals (R7)** — closes the limitation hit 3x during the Connection Pack. Declarative directives in recipe JSON (recipes stay data, not code): `"$if"` conditions (`name` / `!name` / `name=value` / `name!=value`) drop phases, fields, or array elements; `{"$if", "$value"}` envelope for conditional scalar fields; transitions auto-rewire through dropped phases like unlinking a list node (chains of consecutive drops followed; a ref with nowhere to go is a compile error); `"$repeat"` inside `phases` expands one phase per array-param item with `${item.x}` / `${i}` / `${n}` / `${nextKey}` scope vars; `{"$map"}` builds derived arrays (quiz-show leaderboard summing every generated round); placeholders gained dotted paths (`${item.question}`, `${topic.title[0]}`). New `object` parameter type with `fields` (schema validation, coercion, synthetic defaults) + picker card widget (one labeled input per field; array sub-fields are comma-separated). Recipe upgrades that the old compiler couldn't express: One Voice `maxAttempts` (declared-but-blank optional params are falsy in conditions), Snowball `snowballAgain` (pairs → groups of 4 via a second conditional merge; share phase picked via drop-chain rewiring), Closer `partners` (new-each-tier rotatePairsFrom vs same-all-game reusePairsFrom). **Quiz Show shipped** — the recipe deferred since R3: teacher writes N questions (question/choices/correct cards in the picker), compiler emits N chained collect-choice rounds with optional Kahoot-style speed bonus + leaderboard; robot playthrough green (0 errors). All recipes verified compiling in both branches with 0 validation errors. Tests: `recipe-conditionals.test.js`. Test count: **685** (+16).
- 2026-06-10: **Two new phase types — buzz (24th) and estimate (25th)** — picked from a phase-ideas review as the highest classroom-energy-per-effort gaps. **buzz**: first-tap-wins buzzer rounds (trivia bee, spelling bee) — teacher asks questions out loud, first player to tap locks the buzzer (socket arrival order IS the buzz order — server-authoritative, pure referee `createBuzzState`/`applyBuzz`/`applyJudge`/`applyNextQuestion` in the handler), teacher judges ✓/✗ on the host (wrong = lockout for the question + reopen), "Next question" clears lockouts so one phase runs a whole informal round; outputs `scores` (scoreMap). Player UI is one giant red BUZZ button. **estimate**: numeric guessing — optional `answer` + `unit`, players resubmit freely until close, reveal shows the answer + class distribution + stats; scoring is rank-by-closeness so it is scale-free (`closest` winner-takes-all with tie-sharing, or `graduated` linear falloff — pure `engine/phases/estimate-scoring.js`); no answer = poll-the-room mode (stats only). Both: schema entries (new `number` field type), full editor support (catalog, sidebar forms, primary fields), Simple-view sentences, juice cues, simulator coverage (universal sim + robot playtest), connection-family forbidden (they award points). New game **Lightning Round** (buzz → estimate ×2 → leaderboard summing all three). Verified: robot playthrough green (0 errors, cross-phase score summing correct), live screenshot of host judging + both player states. Phase-ideas shortlist (branching votes, match, sort, secret-role, appreciation) added to NEXT-STEPS. Test count: **703** (+18).
- 2026-06-10: **Branching votes — vote.nextByWinner routes the game by outcome** — the biggest structural gap from the phase-ideas review: `next` can now depend on what the class decided. A pick-one vote on a LITERAL option list maps each option's text to a destination phase (`"nextByWinner": {"Enter the cave": "cave"}`); the winner routes the game, unmapped winners fall back to `next`, tie-breaks are deterministic. Wired everywhere a transition lives: state-machine legal-transition graph (first live run crashed without it — regression-tested), validator (target existence, key⊆options, ≥2 options, ref-candidates warning), unreachable-BFS + cycle detection (branches must go forward), recipe-compiler drop-rewiring, editor delete-relink + client validation. Vote phases also gained **teacher-typed literal options** (the "own options" odds-and-ends item) with rank-style list editing plus per-option "If this wins →" phase dropdowns; Simple view shows branches as sentences ("''Climb the spiral staircase'' wins → step 5"). AI generator taught the CYOA pattern. **Two latent bugs found and fixed**: (1) a late "Close Voting" click after the all-votes-in auto-advance ran the tally against the NEXT phase''s state and crashed the whole server — a real-classroom hazard (now kind-guarded + idempotent); (2) the universal simulator''s vote support never worked (listened for "vote-started"/emitted "cast-vote" — real events are "vote-start"/"submit-vote"), which explains old mid-game stalls in vote games. New game **Story Quest: The Locked Library** — class-steered CYOA, 2 branching votes, converging finale; robot playthrough green end-to-end. Test count: **712** (+9).
- 2026-06-10: **Hardening sweep (from the critical self-review)** — three fixes. (1) **Crash-race audit**: the close-voting crash class existed in rank/rate/wager too (closeRanking would iterate the NEXT phase''s state after an all-ranked auto-close raced a late host click). Every stateful phase now stamps `kind` on `room.phaseState`; all close/submit/resolve socket handlers kind-guard; closers + resolveWager are idempotent (`closed`/`resolved` flags). Plus a **global socket error trap** (every handler wrapped — one throwing room can''t kill every classroom on the server) and `process.on('unhandledRejection')` logs instead of crashing. (2) **Filter gaps**: relay text was broadcast unfiltered — `relay-submit` now passes the blocklist + 280-char cap (merge drafts were already filtered, contrary to the review''s claim — but their rejection notice yanked players to the wrong screen). `response-rejected` on the player is now section-aware: merge rejections show in the draft status, relay rejections reopen the turn to revise, collect behaves as before. (3) **CI**: `.github/workflows/test.yml` runs the suite on every push/PR — previously a push deployed to Render with zero automated gate. Verified: 712 tests green, dream-vacation robot playthrough (rank close + relay filter paths) clean.
- 2026-06-10: **Room snapshots — games survive server restarts (pre-August plan, item A)** — the existential gap: all room state lived in memory, so a Render redeploy/sleep/crash at minute 20 of a period silently killed every running game. Now every phase transition snapshots the room to Neon (`engine/room-snapshot.js`, `room_snapshots` table: engine position, phaseData, players + tokens, eliminations, kicked tokens, teacherPin, debounced 300ms, TTL-swept at 6h). A restarted server lazily resurrects the room when anyone rejoins: players via the existing token rebind; the host via a new `hostToken` (sessionStorage → `host-rejoin` on connect). Semantic: resume at the START of the interrupted phase (that phase''s mid-progress is lost; the game is not). Mid-foreach restores re-enter the foreach parent fresh. **Bonus fix**: host disconnect used to DELETE the room instantly — a teacher''s F5 or flaky projector laptop killed the game for the whole class; now the room holds 5 minutes for rejoin, and a host-rejoin re-enters the current phase. Wrong hostToken rejected; `?game=`/prototype launches never auto-rebind; `_sim-tmp-` rooms never persist. CI now supports **deploy-on-green** (RENDER_DEPLOY_HOOK secret + Render auto-deploy off — manual setup steps in the workflow comment). Verified end-to-end by `scripts/simulate-restart.js`: plays to mid-collect, SIGKILLs the server, starts a fresh one, host+player rejoin — roster, phase, and PIN all survive (9/9 checks). Test count: **716** (+4).
- 2026-06-10: **Chaos simulator (pre-August plan, item B) — and the five real bugs it found** — `scripts/simulate-chaos.js` + chaos mode in `services/simulator.js`: while the robot reactor plays a game, players randomly drop and reconnect mid-phase (token rebind), ghosts join with dead tokens, and stale/malformed/duplicate events spray the server; the game must still complete with every reconnect recognized. Suite covers all interactive phase types with zero AI spend (lightning-round, story-quest, snowball, one-voice, dream-vacation, charades-bowl). Fixed what it caught: (1) **player-id migration on reconnect** (`engine/id-migration.js`) — phases store player ids that went stale on rebind: turn describers got dead buttons after a wifi blip, relay players lost their turn, votes were silently dropped, and completed-phase score maps orphaned (reconnected kids vanished from leaderboards — a long-standing weakness now gone; deep in-place rewrite through phaseState/phaseData/foreachState); (2) **generic advance-phase now closes the current phase first** — the teacher console''s "Next step" used to blow past closeOneVoice/tally/closeRanking etc., losing outputs and leaving raw `{{tokens}}` on the next screen; (3) **close-submissions phase guard** — a stray/late close stored empty responses under whatever phase was current; (4) estimate reconnect-after-close shows results, and ESTIMATE_RESULTS/buzz broadcasts carry `phaseInstanceId`; (5) **empty rank/vote phases skip** instead of stranding students on an empty ballot. Sim-side fixes: rank-start host-payload false positive; charades "timeout" was a duplicate-phrase once-key in the reactor (game finishes in ~25s under full chaos, no special-casing needed). Final run: 6/6 games complete under maximum hostility — 59 reconnects (0 unrecognized), 18 ghosts, dozens of hostile payloads. Test count: **720** (+4 id-migration).
